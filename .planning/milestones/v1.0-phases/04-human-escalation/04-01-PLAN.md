---
phase: 04-human-escalation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - messenger-bot/src/tests/escalation.test.ts
  - messenger-bot/.env.example
autonomous: true
requirements: [ESC-01, ESC-02, ESC-03, ESC-04]

must_haves:
  truths:
    - "A test file exists exercising ESC-01 quick_reply path, ESC-01 postback path, ESC-02 admin notification, ESC-03 passThreadControl Graph API call, ESC-04 last-message context, and the ADMIN_PSID soft-fail"
    - "Every escalation test skips gracefully (t.skip with 'pending Plan 02') when the Wave 1 exports are not yet present, matching the handlers.test.ts pattern"
    - "messenger-bot/.env.example lists ADMIN_PSID with the comment explaining where to find it"
    - "`cd messenger-bot && npm test` exits zero with the new escalation tests reported as skipped (not failed)"
  artifacts:
    - path: "messenger-bot/src/tests/escalation.test.ts"
      provides: "Failing/skipping test stubs for ESC-01, ESC-02, ESC-03, ESC-04, soft-fail"
      contains: "handleEscalation"
      contains_also: "pass_thread_control"
      contains_also2: "lastMessage"
    - path: "messenger-bot/.env.example"
      provides: "ADMIN_PSID documented optional env var"
      contains: "ADMIN_PSID"
  key_links:
    - from: "messenger-bot/src/tests/escalation.test.ts"
      to: "messenger-bot/src/index.ts"
      via: "require('../index') with typeof === 'function' guard"
      pattern: "require\\(.*index.*\\)"
    - from: "messenger-bot/.env.example"
      to: "messenger-bot/src/index.ts ADMIN_PSID consumer (Wave 1)"
      via: "process.env.ADMIN_PSID"
      pattern: "ADMIN_PSID"
---

<objective>
Wave 0 of the Human Escalation MVP slice. Land failing/skipping test stubs that pin the behavior contract for the four ESC requirements and the ADMIN_PSID soft-fail, and document the new ADMIN_PSID env var so Plan 02 has everything it needs to flip the tests GREEN without writing new tests of its own.

Purpose: Lock the acceptance shape before any implementation exists, in line with the project's vertical-slice MVP rule (failing end-to-end test first). The tests serve as the executable spec for Plan 02.

Output: `messenger-bot/src/tests/escalation.test.ts` (skips until Wave 1 exports land) and an updated `messenger-bot/.env.example`.
</objective>

## Phase Goal

**As a** Govi customer who cannot find the answer in the menu, **I want to** tap "Contact Human" from any screen, **so that** a real person at Govi sees my last message and replies to me directly inside Messenger.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-human-escalation/04-RESEARCH.md
@.planning/phases/04-human-escalation/04-VALIDATION.md

@messenger-bot/src/index.ts
@messenger-bot/src/tests/handlers.test.ts
@messenger-bot/.env.example
@messenger-bot/package.json

<interfaces>
<!-- Names Wave 1 will export from messenger-bot/src/index.ts. Wave 0 tests probe -->
<!-- for these at require-time; missing exports => t.skip("pending Plan 02 implementation"). -->

From messenger-bot/src/index.ts (existing, already exported):
  export const GRAPH_API_VERSION = "v21.0";
  export async function sendMessage(recipientId: string, text: string, quickReplies?: QuickReply[]): Promise<void>;
  export async function handleWebhookEvent(event: any): Promise<void>;
  export const MAIN_MENU_QUICK_REPLIES: QuickReply[];  // already contains MENU_CONTACT_HUMAN

From messenger-bot/src/index.ts (Plan 02 will add):
  export const PAGE_INBOX_APP_ID = "263902037430900";
  export const lastMessageCache: Map<string, string>;
  export async function passThreadControl(recipientId: string): Promise<void>;
  export async function handleEscalation(senderId: string): Promise<void>;

Test runner (from messenger-bot/package.json):
  npm test => node --test-force-exit --test --require ts-node/register src/tests/*.test.ts
  PORT=0 in test files prevents index.ts boot collision with port 3000.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ADMIN_PSID to messenger-bot/.env.example</name>
  <read_first>
    - messenger-bot/.env.example (current shape — note FACEBOOK_APP_SECRET comment style)
    - .planning/phases/04-human-escalation/04-RESEARCH.md (section: Admin PSID Discovery)
  </read_first>
  <files>messenger-bot/.env.example</files>
  <action>
    Append a new optional env var ADMIN_PSID to messenger-bot/.env.example, matching the existing comment style (em-dash, single-line, names where to find the value). Place it after the FACEBOOK_APP_SECRET block and before GOVI_AI_URL. The comment must explain that the value is the admin's Page-Scoped ID (obtained by having the admin message the Page once and reading `[senderId]` from the bot log). Use a placeholder value of `your_admin_psid_here`. Do not introduce any other changes to the file. Per ESC-02 and the RESEARCH "Admin PSID Discovery" steps.
  </action>
  <acceptance_criteria>
    - `grep -c '^ADMIN_PSID=' messenger-bot/.env.example` returns `1`
    - `grep -B1 '^ADMIN_PSID=' messenger-bot/.env.example | head -1 | grep -q '^#'` (a comment line precedes ADMIN_PSID)
    - The comment line mentions either "PSID" or "Page-Scoped" so the meaning is unambiguous: `grep -B1 '^ADMIN_PSID=' messenger-bot/.env.example | grep -iE 'psid|page-scoped'`
    - No existing keys removed: `grep -E '^(FACEBOOK_VERIFY_TOKEN|FACEBOOK_PAGE_ACCESS_TOKEN|FACEBOOK_APP_SECRET|GOVI_AI_URL|PORT)=' messenger-bot/.env.example | wc -l` returns `5`
  </acceptance_criteria>
  <verify>
    <automated>grep -c '^ADMIN_PSID=' messenger-bot/.env.example | grep -q '^1$' && grep -B1 '^ADMIN_PSID=' messenger-bot/.env.example | grep -iqE 'psid|page-scoped' && [ "$(grep -cE '^(FACEBOOK_VERIFY_TOKEN|FACEBOOK_PAGE_ACCESS_TOKEN|FACEBOOK_APP_SECRET|GOVI_AI_URL|PORT)=' messenger-bot/.env.example)" = "5" ] && echo OK</automated>
  </verify>
  <done>ADMIN_PSID line is present in .env.example with a discovery-instructions comment; the five pre-existing env keys are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Create messenger-bot/src/tests/escalation.test.ts with skipping stubs for ESC-01/02/03/04 and ADMIN_PSID soft-fail</name>
  <read_first>
    - messenger-bot/src/tests/handlers.test.ts (full file — the require()-with-typeof-guard skip pattern is the template)
    - messenger-bot/src/index.ts (lines 255–311 handleWebhookEvent dispatcher; lines 142–145 MAIN_MENU_QUICK_REPLIES)
    - .planning/phases/04-human-escalation/04-RESEARCH.md (Patterns 1–4, Pitfall 1 ordering rule, Validation Architecture test map)
    - .planning/phases/04-human-escalation/04-VALIDATION.md (per-task verification map)
    - messenger-bot/package.json (test script — confirms ts-node/register)
  </read_first>
  <files>messenger-bot/src/tests/escalation.test.ts</files>
  <action>
    Create the file messenger-bot/src/tests/escalation.test.ts. Mirror the structure of handlers.test.ts exactly: top-of-file `process.env.PORT = "0";`, `import { test } from "node:test";`, `import assert from "node:assert/strict";`, type-only side-effect import from "../index", then a `try { const mod = require("../index"); ... } catch { ... }` block that probes typeof for each Wave-1 export and assigns undefined fallbacks.

    Probe these named exports from "../index": `handleEscalation`, `passThreadControl`, `handleWebhookEvent`, `lastMessageCache`, `PAGE_INBOX_APP_ID`. For `lastMessageCache` check `mod.lastMessageCache instanceof Map`; for `PAGE_INBOX_APP_ID` check `typeof === "string"`. The others use the `typeof === "function"` check.

    Write the following tests. Every test begins with the corresponding guard `if (!<binding>) { t.skip("pending Plan 02 implementation"); return; }` (use t.skip exactly as handlers.test.ts does — not assert-based skips). Inside each test, save and restore `axios.post` (and `axios.get` where needed) the same way handlers.test.ts does, and capture calls into a typed array of `{ url: string; body: any }`. Process env mutations (setting/unsetting ADMIN_PSID) must be restored in `finally` to avoid leaking into other tests.

    Tests to author (one per bullet — ESC ids in the test name so grep audits map to requirements):

    - "ESC-01: MENU_CONTACT_HUMAN quick_reply invokes handleEscalation flow" — fire `handleWebhookEvent({ message: { quick_reply: { payload: "MENU_CONTACT_HUMAN" }, text: "Contact Human" }, sender: { id: "USR_ESC_1" } })` with ADMIN_PSID set to "ADM_1". Assert at least one axios.post call targets `ADM_1` (admin notification) and at least one targets `USR_ESC_1` (customer confirmation) — both `recipient.id` values must appear in the captured calls. Assert no fallback text ("I work best with the buttons below") leaks into any captured body.

    - "ESC-01: MENU_CONTACT_HUMAN postback invokes handleEscalation flow" — same as above but with `handleWebhookEvent({ postback: { payload: "MENU_CONTACT_HUMAN" }, sender: { id: "USR_ESC_2" } })`. Assert ADM_1 and USR_ESC_2 both appear as recipient ids in captured axios.post calls.

    - "ESC-02: handleEscalation posts an admin notification to ADMIN_PSID" — directly call `handleEscalation("USR_ESC_3")` with ADMIN_PSID="ADM_2"; assert at least one axios.post call has body `recipient.id === "ADM_2"` and `message.text` contains a non-empty string of >=10 chars.

    - "ESC-03: passThreadControl POSTs to /me/pass_thread_control with target_app_id 263902037430900" — directly call `passThreadControl("USR_ESC_4")`; capture the axios.post URL and body. Assert URL string contains `/me/pass_thread_control` and `v21.0`; assert body equals `{ recipient: { id: "USR_ESC_4" }, target_app_id: "263902037430900" }` (use `assert.deepStrictEqual`). Use the exported `PAGE_INBOX_APP_ID` constant for the expected value so any future drift is caught: `assert.strictEqual(PAGE_INBOX_APP_ID, "263902037430900")` as a sanity check at the top of the test.

    - "ESC-04: admin notification body includes the customer's last text message" — pre-populate `lastMessageCache.set("USR_ESC_5", "where is my order")`, then call `handleEscalation("USR_ESC_5")` with ADMIN_PSID="ADM_3". Assert the captured admin-bound message.text contains the substring `where is my order`.

    - "ESC-04: lastMessageCache is updated when handleWebhookEvent receives free text, NOT when it receives a quick_reply" — clear the Map at the start; fire a `message.text` event for USR_ESC_6 with text "real free text"; assert `lastMessageCache.get("USR_ESC_6") === "real free text"`. Then clear the Map again; fire a `message.quick_reply` event for USR_ESC_7 whose `text` is "Contact Human" (the quick-reply button label); assert `lastMessageCache.get("USR_ESC_7") === undefined`. Stub `axios.get` and `axios.post` to no-op so the dispatcher does not fail when it later branches into menu/escalation code.

    - "Soft-fail: ADMIN_PSID absent → handleEscalation sends customer fallback and does NOT crash or call pass_thread_control" — `delete process.env.ADMIN_PSID`. Call `handleEscalation("USR_ESC_8")` and assert it resolves (await does not throw). Assert exactly one axios.post is captured, with `recipient.id === "USR_ESC_8"` and message.text containing the substring "be in touch" (case-insensitive). Assert NO axios.post hit a URL containing `pass_thread_control`. Restore ADMIN_PSID in `finally`.

    Follow project conventions: 2-space indent, camelCase locals, no superfluous comments, `// eslint-disable-next-line @typescript-eslint/no-require-imports` above each `require("axios")` line (matches handlers.test.ts). Do not introduce any new dependencies or test helpers.
  </action>
  <acceptance_criteria>
    - File exists: `test -f messenger-bot/src/tests/escalation.test.ts`
    - Contains the contract anchor strings (the VALIDATION map greps depend on these):
        - `grep -q 'handleEscalation' messenger-bot/src/tests/escalation.test.ts`
        - `grep -q 'pass_thread_control' messenger-bot/src/tests/escalation.test.ts`
        - `grep -q 'lastMessage' messenger-bot/src/tests/escalation.test.ts`
        - `grep -q '263902037430900' messenger-bot/src/tests/escalation.test.ts`
    - All seven test names are present (one per ESC bullet + soft-fail):
        `grep -cE 'test\("(ESC-01|ESC-02|ESC-03|ESC-04|Soft-fail)' messenger-bot/src/tests/escalation.test.ts` returns `7`
    - Skip-guard pattern preserved (matches handlers.test.ts shape):
        `grep -c 't.skip("pending Plan 02 implementation")' messenger-bot/src/tests/escalation.test.ts` is `>= 7`
    - The require/typeof probe block exists exactly once:
        `grep -c 'require("../index")' messenger-bot/src/tests/escalation.test.ts` is `>= 1`
    - `cd messenger-bot && npm test` exits zero. The new escalation tests report as skipped (filtered grep below). Existing handlers tests are unaffected.
  </acceptance_criteria>
  <verify>
    <automated>cd messenger-bot && npm test 2>&1 | tee /tmp/esc-wave0.out && grep -qE '# skipped [1-9]' /tmp/esc-wave0.out && grep -q 'handleEscalation' src/tests/escalation.test.ts && grep -q 'pass_thread_control' src/tests/escalation.test.ts && grep -q 'lastMessage' src/tests/escalation.test.ts && grep -q '263902037430900' src/tests/escalation.test.ts && [ "$(grep -cE 'test\(\"(ESC-01|ESC-02|ESC-03|ESC-04|Soft-fail)' src/tests/escalation.test.ts)" = "7" ] && echo OK</automated>
  </verify>
  <done>escalation.test.ts compiles via ts-node, all 7 tests skip cleanly (Wave 1 exports absent), and the grep gates from VALIDATION.md pass. No existing handlers.test.ts test status changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tests → bot module under test | Test file `require()`s ../index.ts; if the module's top-level code crashes on import (e.g. unconditional ADMIN_PSID dereference), every test fails. Probe must remain inside try/catch like handlers.test.ts. |
| Tests → axios global | Tests monkey-patch `axios.post`/`axios.get`. If not restored in `finally`, downstream tests in the same file or `*.test.ts` siblings break. |
| .env.example → repo | Example file is committed; it must never contain a real PSID, only a placeholder. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Denial of Service (during test) | Test file's require("../index") at top level | mitigate | Wrap in try/catch identical to handlers.test.ts so a missing Wave-1 export skips rather than crashes the runner (acceptance: `grep -c 'try {' escalation.test.ts >= 1`) |
| T-4-04 | Information Disclosure | .env.example | mitigate | Placeholder value `your_admin_psid_here` only; comment names how to obtain the real PSID but does not embed one. ADMIN_PSID is treated as secret-shaped like PAGE_ACCESS_TOKEN. Tests assert against literal strings "ADM_1"/"ADM_2"/"ADM_3", never real ids. |
| T-4-test-env | Tampering | Test process.env mutations | mitigate | Every test that mutates `process.env.ADMIN_PSID` restores it in `finally` so adjacent tests are not contaminated. |
</threat_model>

<verification>
1. `cd messenger-bot && npm test` exits zero with skipped count >= 7 attributed to the new file.
2. `grep -E '^ADMIN_PSID=' messenger-bot/.env.example` returns one match with a placeholder value.
3. The four VALIDATION.md grep gates (handleEscalation, pass_thread_control, lastMessage, 263902037430900) all return matches against escalation.test.ts.
4. No production code paths under `messenger-bot/src/index.ts` are touched in this plan.
</verification>

<success_criteria>
- ADMIN_PSID documented in .env.example with discovery comment
- escalation.test.ts contains 7 test cases (ESC-01 quick_reply, ESC-01 postback, ESC-02 admin notification, ESC-03 pass_thread_control body, ESC-04 last-message context, ESC-04 cache-only-on-free-text, Soft-fail) all currently skipped
- npm test stays green (no regressions in handlers.test.ts)
- Plan 02 has a precise executable spec — every ESC requirement now has a falsifiable assertion
</success_criteria>

<output>
After completion, create `.planning/phases/04-human-escalation/04-01-SUMMARY.md` recording: skipped test count, grep-gate output, the exact list of exports the tests probe for (so Plan 02 knows the contract surface).
</output>
