---
phase: 01-security-bot-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .gitignore
  - messenger-bot/.env.example
  - messenger-bot/package.json
  - messenger-bot/tsconfig.json
  - messenger-bot/src/tests/hmac.test.ts
  - messenger-bot/src/tests/sendMessage.test.ts
  - messenger-bot/src/tests/handlers.test.ts
  - messenger-bot/src/tests/setup.test.ts
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04
user_setup:
  - service: facebook-developer-app
    why: "FACEBOOK_APP_SECRET is required for SEC-01 webhook signature verification; absent → bot warns + skips verification per D-03"
    env_vars:
      - name: FACEBOOK_APP_SECRET
        source: "Facebook Developer Dashboard → Your App → App Settings → Basic → App Secret (Show)"
    dashboard_config: []

must_haves:
  truths:
    - "Running `node --test messenger-bot/src/tests/` exits 0 with the Wave-0 RED-state expectations met (tests skip or assert TBD until Plans 02-03 implement the behavior; runner itself is green)"
    - "`messenger-bot/node_modules` is no longer tracked by git (git ls-files reports nothing under that path)"
    - "`messenger-bot/.env.example` contains a `FACEBOOK_APP_SECRET=` line"
    - "`messenger-bot/package.json` has a `test` script that invokes `node --test` with the ts-node loader"
  artifacts:
    - path: "messenger-bot/src/tests/hmac.test.ts"
      provides: "SEC-01 unit test scaffolds — valid HMAC, invalid HMAC, missing APP_SECRET"
      contains: "verifySignature"
    - path: "messenger-bot/src/tests/sendMessage.test.ts"
      provides: "SEC-02 + SEC-03 unit test scaffolds — error in data, no token in catch logs"
      contains: "sendMessage"
    - path: "messenger-bot/src/tests/handlers.test.ts"
      provides: "CORE-01, CORE-03, CORE-04 unit test scaffolds — GET_STARTED, quick replies, fallback"
      contains: "GET_STARTED"
    - path: "messenger-bot/src/tests/setup.test.ts"
      provides: "CORE-02 unit test scaffold — Messenger profile setup payload"
      contains: "setupMessengerProfile"
    - path: "messenger-bot/.env.example"
      provides: "FACEBOOK_APP_SECRET documented for new developers"
      contains: "FACEBOOK_APP_SECRET="
    - path: ".gitignore"
      provides: "node_modules ignored across the repo"
      contains: "node_modules/"
    - path: "messenger-bot/package.json"
      provides: "npm test wired to node:test runner"
      contains: "\"test\":"
  key_links:
    - from: "messenger-bot/package.json"
      to: "messenger-bot/src/tests/"
      via: "npm test script invoking node --test with ts-node loader"
      pattern: "node --test"
    - from: "messenger-bot/src/tests/*.test.ts"
      to: "messenger-bot/src/index.ts"
      via: "import of exported handlers (Plans 02 + 03 add the exports)"
      pattern: "from \"\\.\\./index\""
---

<objective>
Establish the Wave-0 foundations the rest of Phase 1 depends on:
1. **node_modules git cleanup (D-09)** — add `node_modules/` to `.gitignore` and untrack the already-committed `messenger-bot/node_modules/` directory.
2. **`.env.example` documentation (D-04)** — add `FACEBOOK_APP_SECRET=` with a where-to-find-it comment so a fresh clone knows what to set.
3. **Test runner wiring** — add a `test` script to `messenger-bot/package.json` that runs Node's built-in `node:test` with `ts-node` to execute `.ts` test files directly (zero new npm deps per RESEARCH.md).
4. **Failing test scaffolds (the RED phase of MVP vertical slicing)** — create four test files under `messenger-bot/src/tests/` that declare the behaviors Plans 02 and 03 will make green: HMAC verification (SEC-01), Graph API error detection (SEC-02), safe error logging (SEC-03), Get Started postback (CORE-01), Messenger profile setup payload (CORE-02), quick-reply attachment (CORE-03), and free-text fallback (CORE-04).

Purpose: Per VALIDATION.md (`nyquist_validation: true`), every Phase 1 task in Plans 02 and 03 has an `<automated>` verify command. Those commands point at test files that must exist before the implementation tasks run. This plan creates the scaffolds and the runner. It also clears the only piece of repo tech debt that touches non-source files (D-09).

Output:
- Working `npm test` script in `messenger-bot/`
- Four `.test.ts` files in `messenger-bot/src/tests/` with `node:test` skeletons referencing the symbols Plans 02 and 03 will export from `messenger-bot/src/index.ts`
- `.gitignore` updated; `messenger-bot/node_modules` no longer tracked
- `messenger-bot/.env.example` documents `FACEBOOK_APP_SECRET`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-security-bot-foundation/01-CONTEXT.md
@.planning/phases/01-security-bot-foundation/01-RESEARCH.md
@.planning/phases/01-security-bot-foundation/01-PATTERNS.md
@.planning/phases/01-security-bot-foundation/01-VALIDATION.md
@.planning/phases/01-security-bot-foundation/SKELETON.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
@messenger-bot/src/index.ts
@messenger-bot/.env.example
@messenger-bot/package.json
@messenger-bot/tsconfig.json
@.gitignore

<interfaces>
<!-- Symbols Plans 02 and 03 will export from messenger-bot/src/index.ts.       -->
<!-- These tests import them — the imports will fail until Plans 02/03 export.  -->
<!-- Wave-0 tests use `test.skip(...)` or `assert.ok(true, "TODO: Plan 02")`     -->
<!-- so the suite is GREEN at the runner level while the behavior is RED.       -->

From messenger-bot/src/index.ts (after Plan 02 lands):
```typescript
export function verifySignature(req: Request, res: Response, next: NextFunction): void;
export async function sendMessage(recipientId: string, text: string, quickReplies?: QuickReply[]): Promise<void>;
export interface QuickReply {
  content_type: "text";
  title: string;   // ≤20 chars
  payload: string; // ≤1000 chars
}
```

From messenger-bot/src/index.ts (after Plan 03 lands):
```typescript
export async function setupMessengerProfile(): Promise<void>;
export async function sendWelcomeMessage(recipientId: string): Promise<void>;
export async function sendFallbackMessage(recipientId: string): Promise<void>;
export const MAIN_MENU_QUICK_REPLIES: QuickReply[];
export async function handleWebhookEvent(event: any): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up node_modules tracking + document FACEBOOK_APP_SECRET in .env.example</name>
  <files>.gitignore, messenger-bot/.env.example</files>
  <read_first>
    - .gitignore (current contents — 13 lines; no `node_modules/` entry)
    - messenger-bot/.env.example (current 4 lines; no FACEBOOK_APP_SECRET entry)
    - .planning/phases/01-security-bot-foundation/01-PATTERNS.md (Change Group F and Change Group G — verbatim target state)
    - .planning/phases/01-security-bot-foundation/01-CONTEXT.md (D-04 and D-09)
  </read_first>
  <action>
    Edit `.gitignore`: append two trailing-slash directory entries after the existing `.DS_Store` line — `node_modules/` and `messenger-bot/node_modules/`. Match the existing trailing-newline style. Do not reorder or modify the 13 existing entries (per CLAUDE.md "surgical changes").

    Edit `messenger-bot/.env.example`: insert a new line immediately after `FACEBOOK_PAGE_ACCESS_TOKEN=your_page_access_token_here` (D-04). The new line is exactly `FACEBOOK_APP_SECRET=your_app_secret_here`. Above the new line, add a single-line comment in the existing file's style: `# FACEBOOK_APP_SECRET — Facebook Developer Dashboard → App Settings → Basic → App Secret`. Leave `GOVI_AI_URL` and `PORT` lines untouched.

    Run `git rm -r --cached messenger-bot/node_modules` to stop tracking the already-committed directory (D-09). Do NOT delete the directory on disk — the executor verifies this by listing it after the command. Do NOT commit yet — execute-plan handles the commit step.
  </action>
  <verify>
    <automated>grep -q '^node_modules/$' .gitignore &amp;&amp; grep -q '^messenger-bot/node_modules/$' .gitignore &amp;&amp; grep -q '^FACEBOOK_APP_SECRET=' messenger-bot/.env.example &amp;&amp; test -d messenger-bot/node_modules &amp;&amp; ! git ls-files --error-unmatch messenger-bot/node_modules &gt;/dev/null 2&gt;&amp;1 &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` line count is 15 (was 13) and lines `node_modules/` and `messenger-bot/node_modules/` are both present
    - `messenger-bot/.env.example` contains a line starting with `FACEBOOK_APP_SECRET=` and an adjacent comment line mentioning "Facebook Developer Dashboard"
    - `git ls-files messenger-bot/node_modules | wc -l` reports `0` (was non-zero before)
    - `test -d messenger-bot/node_modules` exits 0 (directory still exists on disk; only untracked)
    - No changes to `messenger-bot/src/index.ts`, `messenger-bot/package.json`, or any other file
  </acceptance_criteria>
  <done>
    `.gitignore` and `messenger-bot/.env.example` updated; `messenger-bot/node_modules` no longer tracked by git but still present on disk; D-04 and D-09 satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire node:test runner into messenger-bot/package.json</name>
  <files>messenger-bot/package.json, messenger-bot/tsconfig.json</files>
  <read_first>
    - messenger-bot/package.json (current 21 lines — `scripts` has `dev`, `build`, `start` only; deps include `ts-node-dev`, no `ts-node`)
    - messenger-bot/tsconfig.json (compilerOptions for ES2020 + CommonJS, strict, source `src/`, output `dist/`)
    - .planning/phases/01-security-bot-foundation/01-VALIDATION.md (Wave 0 Requirements — line 62: `"test": "node --test src/tests/**/*.test.ts"`)
    - .planning/phases/01-security-bot-foundation/01-RESEARCH.md (lines 605-612 — recommendation: node:test built-in, ts-node loader; line 643 "ts-node --test" pattern)
  </read_first>
  <action>
    Edit `messenger-bot/package.json`. Inside the existing `"scripts"` object (between `"build"` and `"start"` or appended last), add a `"test"` entry that runs `node:test` against TypeScript sources via the `ts-node/esm`-equivalent loader. Concrete value: `"test": "node --import ts-node/register --test src/tests/*.test.ts"`. The `--import ts-node/register` flag registers the ts-node CommonJS hook (the existing project is `module: commonjs`, so the register hook — not the ESM loader — is correct). Do not change other scripts or fields.

    Add `"ts-node": "^10.9.0"` to `devDependencies` (alphabetical order between `@types/node` and `ts-node-dev`). `ts-node-dev` already depends transitively on `ts-node`, so this surfaces the binary; verifier will run `npx ts-node --version` to confirm it resolves.

    Edit `messenger-bot/tsconfig.json` ONLY if `compilerOptions.types` is missing the node types — if `"types"` field exists, ensure it contains `"node"`. If `"types"` field is absent, leave the file alone (ambient `@types/node` already in devDependencies covers it). Make no other tsconfig changes.

    Do not run `npm install` — the executor will do that once after the file changes land. Do not modify `dependencies`, only `devDependencies`.
  </action>
  <verify>
    <automated>cd messenger-bot &amp;&amp; node -e "const p=require('./package.json'); if(!p.scripts.test||!p.scripts.test.includes('node --test')||!p.scripts.test.includes('src/tests')) {console.error('test script wrong:', p.scripts.test); process.exit(1)}; if(!p.devDependencies['ts-node']) {console.error('ts-node missing'); process.exit(1)}; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `messenger-bot/package.json` contains `"test"` script whose string contains both `node --test` and `src/tests`
    - `messenger-bot/package.json` `devDependencies` contains a `"ts-node"` key with a `^10.x` semver
    - `npm install` (run by execute-plan) completes without error and `npx ts-node --version` exits 0 inside `messenger-bot/`
    - `messenger-bot/tsconfig.json` either unchanged or only `types` array modified to include `"node"`
    - Existing `dev`, `build`, `start` scripts in `package.json` are unchanged byte-for-byte
  </acceptance_criteria>
  <done>
    `npm test` from `messenger-bot/` invokes the node:test runner against any `.test.ts` files in `src/tests/`; ts-node binary is available. With no test files yet, the runner is expected to print "no test files" and exit 0 — this is acceptable for Task 2 and will become "tests run" after Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create failing test scaffolds for SEC-01/02/03 and CORE-01/02/03/04</name>
  <files>messenger-bot/src/tests/hmac.test.ts, messenger-bot/src/tests/sendMessage.test.ts, messenger-bot/src/tests/handlers.test.ts, messenger-bot/src/tests/setup.test.ts</files>
  <read_first>
    - messenger-bot/src/index.ts (current 78 lines — what symbols are NOT yet exported)
    - .planning/phases/01-security-bot-foundation/01-VALIDATION.md (Per-Task Verification Map rows 1-01-01 through 1-04-02 — tests referenced by name)
    - .planning/phases/01-security-bot-foundation/01-RESEARCH.md (Pattern 1, 2, 3, 4, 5, 6 — exact assertions each test must encode)
    - .planning/phases/01-security-bot-foundation/01-PATTERNS.md (Change Groups A, B, C, D, E — symbol names and call shapes)
    - .planning/codebase/CONVENTIONS.md (TS 2-space indent; camelCase functions; SCREAMING_SNAKE_CASE module constants)
  </read_first>
  <action>
    Create four test files using Node's built-in `node:test` and `node:assert/strict`. Each test file MUST import from `"../index"` (the symbols don't exist yet — that's the RED state). To keep the runner exit-0 in Wave 0 while implementations are pending, every `test(...)` block in these scaffolds wraps its imports inside a `try/catch` and calls `t.skip("pending Plan 02/03 implementation")` if the import fails. This is the documented Wave-0 RED-acceptable shape.

    `messenger-bot/src/tests/hmac.test.ts` — three `test(...)` blocks named:
      1. "verifySignature: valid X-Hub-Signature-256 calls next()" — references SEC-01 happy path; asserts `next` is called and no `res.sendStatus(403)` (Plan 02 makes green)
      2. "verifySignature: invalid signature returns 403" — references SEC-01 forged-event path; asserts `res.sendStatus(403)` called once, `next` not called
      3. "verifySignature: missing FACEBOOK_APP_SECRET logs warn and calls next()" — references D-03; asserts `console.warn` invoked with text containing "skipping webhook signature verification", `next` is called

    `messenger-bot/src/tests/sendMessage.test.ts` — two `test(...)` blocks:
      1. "sendMessage logs Graph API error when response.data.error present" — SEC-02; mocks `axios.post` to resolve with `{data:{error:{message:"send failed",code:10}}}` and asserts `console.error` called with text containing "Graph API error"
      2. "sendMessage catch block does not log full axios error object (no PAGE_ACCESS_TOKEN leak)" — SEC-03; mocks `axios.post` to reject with an AxiosError whose `config.url` contains `PAGE_ACCESS_TOKEN=secret123`; asserts the joined `console.error` call args do NOT contain the substring `secret123` and DO contain `err.message` value

    `messenger-bot/src/tests/handlers.test.ts` — four `test(...)` blocks:
      1. "handleWebhookEvent: GET_STARTED postback invokes sendWelcomeMessage" — CORE-01; mocks `sendMessage`, dispatches `{postback:{payload:"GET_STARTED"},sender:{id:"USR_1"}}`, asserts `sendMessage` called once with `recipientId === "USR_1"` AND a third arg array with `length &gt;= 1`
      2. "sendWelcomeMessage call includes quick_replies in payload" — CORE-03; mocks `axios.post`, calls `sendWelcomeMessage("USR_1")`, asserts the `axios.post` body has `message.quick_replies` of length ≥ 1 with `content_type === "text"`
      3. "handleWebhookEvent: free text triggers sendFallbackMessage" — CORE-04 happy path; dispatches `{message:{text:"hi"},sender:{id:"USR_1"}}`, asserts `sendFallbackMessage` called
      4. "handleWebhookEvent: quick_reply tap does NOT trigger fallback" — CORE-04 pitfall guard (PITFALLS.md pitfall 6); dispatches `{message:{quick_reply:{payload:"MENU_PRODUCT_HELP"},text:"Product Help"},sender:{id:"USR_1"}}`, asserts `sendFallbackMessage` NOT called

    `messenger-bot/src/tests/setup.test.ts` — one `test(...)` block:
      1. "setupMessengerProfile posts get_started + persistent_menu to /me/messenger_profile" — CORE-02; mocks `axios.post`, calls `setupMessengerProfile()`, asserts:
         - URL ends with `/me/messenger_profile` AND contains `graph.facebook.com/v21.0`
         - Body has `get_started.payload === "GET_STARTED"`
         - Body has `persistent_menu[0].call_to_actions` array length between 1 and 3 (CORE-02 max-3 constraint)
         - Every `persistent_menu[0].call_to_actions[i].title.length &lt;= 20` (RESEARCH.md A2 conservative safe limit)

    Each test file: header comment listing the requirement ID(s) it covers; named imports from `"../index"`; uses `node:test`'s `t.mock.method(...)` for axios/console mocking (Node 26 ships this API). All four files compile under the existing `strict: true` tsconfig.
  </action>
  <verify>
    <automated>cd messenger-bot &amp;&amp; npm test 2&gt;&amp;1 | tee /tmp/gsd-test-out.log; EC=$?; if [ $EC -ne 0 ]; then exit $EC; fi; grep -v '^#' /tmp/gsd-test-out.log | grep -cE '^(ok|# pass|# tests)' &gt;/dev/null &amp;&amp; grep -q 'tests/hmac.test.ts' /tmp/gsd-test-out.log &amp;&amp; grep -q 'tests/sendMessage.test.ts' /tmp/gsd-test-out.log &amp;&amp; grep -q 'tests/handlers.test.ts' /tmp/gsd-test-out.log &amp;&amp; grep -q 'tests/setup.test.ts' /tmp/gsd-test-out.log &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist at the exact paths listed in `<files>`
    - Each file imports at least one symbol via `from "../index"` (executor verifies with `grep -l 'from "../index"' messenger-bot/src/tests/*.test.ts` returning 4 files)
    - `cd messenger-bot &amp;&amp; npm test` exits 0
    - Test runner output mentions all four file paths (`hmac.test.ts`, `sendMessage.test.ts`, `handlers.test.ts`, `setup.test.ts`)
    - Total `test(...)` block count across the four files is exactly 10 (matches VALIDATION.md per-task verification rows)
    - Each test block either passes a placeholder assertion OR calls `t.skip(...)` with a "pending Plan 02" or "pending Plan 03" reason — never errors out at runtime
    - No file outside `messenger-bot/src/tests/` is modified by this task
  </acceptance_criteria>
  <done>
    Wave 0 test scaffolds in place; `npm test` exits 0 and exercises 10 named test blocks across 4 files. Plans 02 and 03 will replace `t.skip(...)` and placeholder assertions with real expectations once they export the symbols.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo ↔ public git remote | `.gitignore` controls what leaves the local repo; secrets and bulky vendor trees must not cross this boundary |
| developer ↔ Facebook Developer Dashboard | `FACEBOOK_APP_SECRET` is fetched from outside the codebase; documentation (`.env.example`) is the only in-repo trust artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-00-01 | Information Disclosure | `.gitignore` — secrets/vendor trees accidentally pushed | mitigate | Add `node_modules/` and `messenger-bot/node_modules/` so future installs cannot push vendored binaries; `.env` already present in `.gitignore` line 1 |
| T-1-00-02 | Information Disclosure | `messenger-bot/.env.example` — committing real secret instead of placeholder | mitigate | Placeholder value `your_app_secret_here` only; reviewer-visible comment documents where to fetch the real value; pre-existing pattern matches other entries |
| T-1-00-03 | Tampering | Test scaffolds — assertion drift across plans | mitigate | Acceptance criteria pin the exact `test(...)` block count (10) and each block name to a requirement ID; Plans 02 and 03 cannot quietly drop a behavior |
| T-1-00-04 | Repudiation | Test runner — silent green on no-tests-found | accept | `npm test` after Task 3 runs 10 named blocks; Wave 0 acceptance verifies block count, not just exit code |
</threat_model>

<verification>
- `cd messenger-bot &amp;&amp; npm test` exits 0 with all four test files referenced in output
- `git ls-files messenger-bot/node_modules` returns no rows
- `grep -E '^node_modules/$|^messenger-bot/node_modules/$' .gitignore` returns 2 lines
- `grep -E '^FACEBOOK_APP_SECRET=' messenger-bot/.env.example` returns 1 line
- `node --import ts-node/register --test messenger-bot/src/tests/hmac.test.ts` runs (does not crash on import)
</verification>

<success_criteria>
- Wave 0 test infrastructure is live: `npm test` runs `node:test` against TypeScript sources via `ts-node/register`
- 10 named `test(...)` blocks exist across 4 files, mapping 1:1 to the VALIDATION.md per-task verification matrix
- `node_modules/` cleanup (D-09) and `FACEBOOK_APP_SECRET` documentation (D-04) are complete
- No production source file is modified — Plans 02 and 03 own all `messenger-bot/src/index.ts` edits
- All four test files are red-state intentionally: they reference symbols that don't yet exist; tests skip rather than fail, so the runner stays green throughout the wave-0→wave-1→wave-2 transition
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-bot-foundation/01-01-SUMMARY.md`
</output>
