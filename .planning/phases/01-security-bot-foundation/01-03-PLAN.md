---
phase: 01-security-bot-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 02
files_modified:
  - messenger-bot/src/index.ts
  - messenger-bot/src/tests/handlers.test.ts
  - messenger-bot/src/tests/setup.test.ts
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04

must_haves:
  truths:
    - "A new user who clicks 'Get Started' on the Facebook Page triggers a postback event with payload 'GET_STARTED' which the bot answers with a welcome message accompanied by quick-reply navigation"
    - "A user who types free text at any point receives a friendly fallback message with the same quick-reply options as the persistent menu — no silent dead end"
    - "A user opening the persistent hamburger menu sees up to 3 top-level options ('Product Help', 'Contact Human', 'Main Menu'), each ≤20 characters"
    - "Tapping a quick reply does NOT trigger the free-text fallback (event.message.quick_reply is dispatched before event.message.text)"
    - "Bot startup calls POST /me/messenger_profile once, sending get_started + persistent_menu; if the call fails, the bot continues running and logs the error safely"
  artifacts:
    - path: "messenger-bot/src/index.ts"
      provides: "Full bot skeleton: postback dispatch + quick-reply guard + fallback handler + Messenger profile setup"
      contains: "setupMessengerProfile"
      exports: ["setupMessengerProfile", "sendWelcomeMessage", "sendFallbackMessage", "MAIN_MENU_QUICK_REPLIES", "handleWebhookEvent"]
      min_lines: 150
  key_links:
    - from: "app.listen callback"
      to: "setupMessengerProfile()"
      via: "fire-and-forget call after Express binds"
      pattern: "setupMessengerProfile\\(\\)"
    - from: "setupMessengerProfile"
      to: "POST https://graph.facebook.com/v21.0/me/messenger_profile"
      via: "axios.post with PAGE_ACCESS_TOKEN query param"
      pattern: "me/messenger_profile"
    - from: "event dispatcher"
      to: "sendWelcomeMessage / sendFallbackMessage"
      via: "postback.payload === 'GET_STARTED' OR message.text → fallback"
      pattern: "GET_STARTED"
    - from: "sendWelcomeMessage / sendFallbackMessage"
      to: "sendMessage(recipient, text, MAIN_MENU_QUICK_REPLIES)"
      via: "shared quick-reply constant"
      pattern: "MAIN_MENU_QUICK_REPLIES"
---

<objective>
Vertical slice 2: **the bot is navigationally complete**. After this plan, a user can open the bot, click Get Started, see a welcome with quick replies, open the hamburger persistent menu, type free text and get a helpful re-anchor message — all the user-visible behaviors of Phase 1's `**Goal**` line.

Four connected pieces, all in `messenger-bot/src/index.ts`:

1. **CORE-02 (Messenger Profile setup)** — Add `setupMessengerProfile()` that calls `POST /me/messenger_profile` once at startup to configure `get_started: { payload: "GET_STARTED" }`, `greeting` (D-05 default copy), and `persistent_menu` with 3 top-level postback items (`Product Help`, `Contact Human`, `Main Menu`). Wired into the existing `app.listen` callback.
2. **CORE-01 + CORE-03 (Welcome message with quick replies)** — Add `sendWelcomeMessage(recipientId)` which calls `sendMessage` (from Plan 02) with welcome copy and the `MAIN_MENU_QUICK_REPLIES` array. Triggered when the event dispatcher sees `event.postback.payload === "GET_STARTED"`.
3. **CORE-04 (Free-text fallback with quick replies)** — Add `sendFallbackMessage(recipientId)` which sends the D-01 friendly re-anchor text + D-02 quick replies mirroring the persistent menu's top level. Triggered when the event dispatcher sees `event.message.text` AND no `event.message.quick_reply`.
4. **Event dispatch refactor (CORE-04 pitfall guard)** — Replace the existing event loop body (which assumes every event is `message.text` and calls Govi AI) with the dispatcher from PATTERNS.md Change Group E: order is `postback` → `quick_reply` → `text`. Quick reply payloads are acknowledged (logged + `continue`) — actual routing arrives in Phase 3. The existing Govi AI `axios.post(${GOVI_AI_URL}/ai/chat, …)` call is REMOVED — Phase 1 ends the AI-passthrough behavior entirely (the AI passthrough is superseded by the Q&A vault flow in Phases 2-3 per ROADMAP.md).

Plus: extract a `GRAPH_API_VERSION` constant so the v21.0 string lives in one place — both `sendMessage` (from Plan 02) and the new `setupMessengerProfile` reference it.

Purpose: This is the second half of the Phase 1 ROADMAP goal — "customers can open the bot, see a welcome message, navigate the persistent menu, and never get a silent dead end". After this plan ALL 10 unit tests are green and ALL 5 ROADMAP success criteria for Phase 1 are satisfied.

Output:
- `messenger-bot/src/index.ts` final form: secure (from Plan 02) + navigationally complete (from this plan)
- 5 new exported symbols: `MAIN_MENU_QUICK_REPLIES`, `setupMessengerProfile`, `sendWelcomeMessage`, `sendFallbackMessage`, `handleWebhookEvent` (the extracted dispatcher used by `handlers.test.ts`)
- `handlers.test.ts` and `setup.test.ts` flipped from RED-skip to GREEN-pass; all 10 tests green
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-security-bot-foundation/01-CONTEXT.md
@.planning/phases/01-security-bot-foundation/01-RESEARCH.md
@.planning/phases/01-security-bot-foundation/01-PATTERNS.md
@.planning/phases/01-security-bot-foundation/01-VALIDATION.md
@.planning/phases/01-security-bot-foundation/SKELETON.md
@.planning/codebase/CONVENTIONS.md
@messenger-bot/src/index.ts
@messenger-bot/src/tests/handlers.test.ts
@messenger-bot/src/tests/setup.test.ts
@.planning/phases/01-security-bot-foundation/01-02-PLAN.md

<interfaces>
Symbols Plan 02 already exports — DO NOT redefine:
- `export function verifySignature(req, res, next): void`
- `export async function sendMessage(recipientId, text, quickReplies?: QuickReply[]): Promise<void>`
- `export interface QuickReply { content_type: "text"; title: string; payload: string; }`

New symbols this plan adds:
- `export const GRAPH_API_VERSION = "v21.0"`
- `export const MAIN_MENU_QUICK_REPLIES: QuickReply[]`
- `export async function setupMessengerProfile(): Promise<void>`
- `export async function sendWelcomeMessage(recipientId: string): Promise<void>`
- `export async function sendFallbackMessage(recipientId: string): Promise<void>`
- `export async function handleWebhookEvent(event: any): Promise<void>`

Plan 02 already removed `app.use(express.json())` and wired `express.raw` + `verifySignature` on POST /webhook. This plan only rewrites the handler body / event loop, not the middleware chain.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Messenger Profile setup (CORE-01 + CORE-02) and call from app.listen</name>
  <files>messenger-bot/src/index.ts, messenger-bot/src/tests/setup.test.ts</files>
  <read_first>
    - messenger-bot/src/index.ts (state after Plan 02 — contains `verifySignature`, exported `sendMessage`, `QuickReply` interface; `app.listen` callback on the final lines)
    - .planning/phases/01-security-bot-foundation/01-PATTERNS.md (Change Group D lines 199-246 — verbatim setupMessengerProfile + call-site pattern)
    - .planning/phases/01-security-bot-foundation/01-RESEARCH.md (Pattern 4 lines 278-329 — endpoint, payload structure, persistent menu constraints; Pitfall 5 line 469 — silent setup failure)
    - .planning/phases/01-security-bot-foundation/01-CONTEXT.md (D-05 — Claude's discretion on copy and menu labels)
    - messenger-bot/src/tests/setup.test.ts (1 test block from Plan 01-01)
  </read_first>
  <behavior>
    - `setupMessengerProfile()` posts to URL containing `graph.facebook.com/${GRAPH_API_VERSION}/me/messenger_profile` with `access_token` query param
    - Posted body has exactly three top-level fields: `get_started`, `greeting`, `persistent_menu`
    - `get_started.payload === "GET_STARTED"`
    - `greeting[0]` has `locale: "default"` and a non-empty `text` string ≤200 characters
    - `persistent_menu[0]` has `locale: "default"`, `composer_input_disabled: false`, and `call_to_actions` array of length exactly 3
    - Each `call_to_actions[i]` has `type === "postback"`, a `title` whose length ≤20 chars, and a `payload` string
    - The three `payload` strings are exactly `"MENU_PRODUCT_HELP"`, `"MENU_CONTACT_HUMAN"`, `"MENU_MAIN"`
    - On axios rejection, function does NOT throw — logs via `console.error("Messenger profile setup failed:", axiosErr.message, axiosErr.response?.data)` and returns
    - On success, function logs `console.log("Messenger profile configured")`
    - `setupMessengerProfile()` is invoked once from inside the existing `app.listen(port, () => { … })` callback, after the existing `console.log` line
    - A module-level `export const GRAPH_API_VERSION = "v21.0"` exists; the `sendMessage` URL string is rewritten as a template literal referencing the constant (single source of truth)
  </behavior>
  <action>
    Open `messenger-bot/src/index.ts`. Three edit groups:

    **Edit Group A — Extract `GRAPH_API_VERSION` constant.** After the constants block (after `APP_SECRET`), add the exported constant `GRAPH_API_VERSION` with value `"v21.0"`. Update the `sendMessage` axios.post URL from the literal `https://graph.facebook.com/v21.0/me/messages` to a template literal referencing `GRAPH_API_VERSION`. Do NOT touch any other `sendMessage` logic.

    **Edit Group B — Add `MAIN_MENU_QUICK_REPLIES` and `setupMessengerProfile`.** After the `sendMessage` function definition, add an exported `MAIN_MENU_QUICK_REPLIES: QuickReply[]` constant with exactly two entries: `{ content_type: "text", title: "Product Help", payload: "MENU_PRODUCT_HELP" }` and `{ content_type: "text", title: "Contact Human", payload: "MENU_CONTACT_HUMAN" }`. The persistent menu has 3 items including "Main Menu", but the inline quick-reply mirror is 2 items per D-02 / D-05 sensible defaults — the third menu item is reachable via the hamburger.

    Then add the `setupMessengerProfile` function from PATTERNS.md Change Group D lines 205-236 with these concrete deviations:
    - The Graph API URL uses the new `GRAPH_API_VERSION` constant via template literal — single source of truth
    - Function is exported
    - `persistent_menu[0].call_to_actions` titles: `"Product Help"` (12 chars), `"Contact Human"` (13 chars), `"Main Menu"` (9 chars) — all ≤20 chars (RESEARCH.md A2 safe limit)
    - `persistent_menu[0].call_to_actions` payloads: `"MENU_PRODUCT_HELP"`, `"MENU_CONTACT_HUMAN"`, `"MENU_MAIN"`
    - Greeting text (D-05 discretion): `"Hi! I'm the Govi support bot. Ask me about products or talk to a human."` — exactly PATTERNS.md text
    - Add a code comment above the greeting array: `// Greeting + persistent-menu copy are placeholders per D-05 — user may adjust before go-live`
    - Try/catch wraps the axios.post. Success path logs `console.log("Messenger profile configured")`. Catch path uses the SEC-03 safe-log shape: `const axiosErr = err as import("axios").AxiosError; console.error("Messenger profile setup failed:", axiosErr.message, axiosErr.response?.data);` — never the bare err.

    **Edit Group C — Call `setupMessengerProfile` from `app.listen`.** Modify the existing `app.listen(port, () => { console.log(...) })` callback to invoke `setupMessengerProfile()` fire-and-forget AFTER the existing port log line. Do NOT await — the callback is sync; the function's own try/catch handles failures.

    Update `messenger-bot/src/tests/setup.test.ts`: remove `t.skip(...)`, implement the single test block from Plan 01-01. Mock `axios.post` resolved with `{ data: { result: "success" } }`. Call `await setupMessengerProfile()`. Assert against `axios.post.mock.calls[0].arguments`:
    - `arguments[0]` (url) ends with `/me/messenger_profile` AND contains `graph.facebook.com/v21.0`
    - `arguments[1].get_started.payload === "GET_STARTED"`
    - `arguments[1].persistent_menu[0].call_to_actions.length === 3`
    - Every `arguments[1].persistent_menu[0].call_to_actions[i].title.length <= 20`
    - Every `arguments[1].persistent_menu[0].call_to_actions[i].type === "postback"`
    - `arguments[2].params.access_token` is defined (property path exists; value may be undefined in tests with no env var)

    Do NOT modify the existing event loop in this task — it stays broken until Task 2. Do NOT touch `verifySignature` or `sendMessage`'s internals from Plan 02.
  </action>
  <verify>
    <automated>cd messenger-bot &amp;&amp; npm test 2&gt;&amp;1 | tee /tmp/gsd-setup-test.log; EC=$?; if [ $EC -ne 0 ]; then exit $EC; fi; grep -v '^#' /tmp/gsd-setup-test.log | grep -E 'setup\.test\.ts' | grep -qE '(ok|pass)' &amp;&amp; grep -q 'export const GRAPH_API_VERSION = "v21.0"' messenger-bot/src/index.ts &amp;&amp; grep -q "export async function setupMessengerProfile" messenger-bot/src/index.ts &amp;&amp; grep -q "/me/messenger_profile" messenger-bot/src/index.ts &amp;&amp; grep -q "MENU_PRODUCT_HELP" messenger-bot/src/index.ts &amp;&amp; grep -q "MENU_CONTACT_HUMAN" messenger-bot/src/index.ts &amp;&amp; grep -q "MENU_MAIN" messenger-bot/src/index.ts &amp;&amp; grep -q "setupMessengerProfile()" messenger-bot/src/index.ts &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `messenger-bot/src/index.ts` contains `export const GRAPH_API_VERSION = "v21.0"`
    - `messenger-bot/src/index.ts` contains exactly ONE occurrence of the literal string `"v21.0"` (the constant; `sendMessage` URL uses template literal)
    - `messenger-bot/src/index.ts` contains `export const MAIN_MENU_QUICK_REPLIES: QuickReply[] = [`
    - `messenger-bot/src/index.ts` contains `export async function setupMessengerProfile`
    - `messenger-bot/src/index.ts` contains the call `setupMessengerProfile()` somewhere AFTER the `app.listen(` line (verified by line-number ordering in grep output)
    - `messenger-bot/src/index.ts` contains the three persistent-menu payload strings: `"MENU_PRODUCT_HELP"`, `"MENU_CONTACT_HUMAN"`, `"MENU_MAIN"`
    - `messenger-bot/src/index.ts` contains the literal `composer_input_disabled: false` (accessibility — D-05 sensible default)
    - All persistent-menu titles in `call_to_actions` are ≤20 chars (asserted by setup.test.ts)
    - `cd messenger-bot &amp;&amp; npm test` exits 0 and 6 tests now pass: 3 HMAC + 2 sendMessage (Plan 02) + 1 setup (this task)
    - `cd messenger-bot &amp;&amp; npm run build` exits 0 (strict TS still clean)
  </acceptance_criteria>
  <done>
    Messenger Profile setup ships and is invoked at startup; `MAIN_MENU_QUICK_REPLIES` constant defined; `GRAPH_API_VERSION` extracted; 6 of 10 tests now green; persistent menu will appear in Messenger once the bot binds to a real Page (manual verification step in <verification>).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor event dispatcher (handleWebhookEvent) for CORE-01 + CORE-03 + CORE-04 with quick-reply guard</name>
  <files>messenger-bot/src/index.ts, messenger-bot/src/tests/handlers.test.ts</files>
  <read_first>
    - messenger-bot/src/index.ts (state after Task 1 above — POST `/webhook` handler still contains the old `message.text → axios.post(GOVI_AI_URL)` event loop from the original file; this task rewrites that body)
    - .planning/phases/01-security-bot-foundation/01-PATTERNS.md (Change Group E lines 250-334 — verbatim event-loop replacement; helper functions `sendWelcomeMessage` and `sendFallbackMessage` lines 288-303)
    - .planning/phases/01-security-bot-foundation/01-RESEARCH.md (Pattern 5 lines 332-362 — dispatcher order: postback → quick_reply → free text; Pattern 6 lines 366-402 — quick reply payload constraints; Pitfall 6 line 478 — quick_reply MUST be checked before text)
    - .planning/phases/01-security-bot-foundation/01-CONTEXT.md (D-01, D-02 — fallback tone and quick replies mirror menu)
    - messenger-bot/src/tests/handlers.test.ts (4 test blocks from Plan 01-01)
  </read_first>
  <behavior>
    - A new exported async function `handleWebhookEvent(event)` encapsulates the dispatch logic — the POST `/webhook` handler's nested `for (const event of ...)` loop now awaits `handleWebhookEvent(event)` rather than inlining the logic. Exporting the dispatcher lets `handlers.test.ts` call it directly without spinning up Express.
    - `handleWebhookEvent` dispatch order, top to bottom (PITFALL 6 — quick_reply BEFORE text):
        1. If `event.postback?.payload === "GET_STARTED"` → `await sendWelcomeMessage(event.sender.id)` and return
        2. Else if `event.postback` (any other payload) → `console.log` the payload and return (placeholder for Phase 3 routing)
        3. Else if `event.message?.quick_reply` → `console.log` the quick_reply payload and return (placeholder — DOES NOT fall through to text branch even though `event.message.text` is also present for quick replies)
        4. Else if `event.message?.text` → log `[senderId] text` (preserve existing logging style) and `await sendFallbackMessage(event.sender.id)` and return
        5. Otherwise: do nothing
    - `sendWelcomeMessage(recipientId)` calls `sendMessage(recipientId, "Welcome to Govi! I can help with product questions or connect you with a human.", MAIN_MENU_QUICK_REPLIES)` — D-05 discretion copy; ≤200 chars
    - `sendFallbackMessage(recipientId)` calls `sendMessage(recipientId, "I work best with the buttons below — here's what I can help with:", MAIN_MENU_QUICK_REPLIES)` — exact text from D-01; uses MAIN_MENU_QUICK_REPLIES per D-02
    - The existing `axios.post(${GOVI_AI_URL}/ai/chat, …)` call is REMOVED — Phase 1 retires the AI passthrough; the `GOVI_AI_URL` constant remains in the file for future phases but is no longer referenced in any function body
    - Existing `res.sendStatus(200)` BEFORE the event loop is preserved byte-for-byte (Facebook's 20s acknowledgement requirement)
    - Existing `if (body.object !== "page") { res.sendStatus(404); return; }` is preserved
  </behavior>
  <action>
    Open `messenger-bot/src/index.ts`. Two edit groups.

    **Edit Group A — Add helper functions and exported event dispatcher.** Immediately after `MAIN_MENU_QUICK_REPLIES` (added in Task 1) and before `setupMessengerProfile`, add three new exported async functions: `sendWelcomeMessage`, `sendFallbackMessage`, `handleWebhookEvent`.

    `sendWelcomeMessage(recipientId)` body: a single `await sendMessage(...)` call with the welcome text `"Welcome to Govi! I can help with product questions or connect you with a human."` and `MAIN_MENU_QUICK_REPLIES` as the third arg.

    `sendFallbackMessage(recipientId)` body: a single `await sendMessage(...)` call with the fallback text `"I work best with the buttons below — here's what I can help with:"` (exact text per D-01, including the em-dash) and `MAIN_MENU_QUICK_REPLIES` as the third arg.

    `handleWebhookEvent(event: any)` implements the 5-step dispatcher described in `<behavior>` above. Use optional-chaining (`event.postback?.payload`, `event.message?.quick_reply`, `event.message?.text`) per the existing codebase style. Pull `senderId` from `event.sender?.id` once at the top; early-return if absent.

    **Edit Group B — Rewrite the POST `/webhook` handler body.** Inside the existing `app.post("/webhook", express.raw({ type: "*/*" }), verifySignature, async (req, res) => { … })` (set up in Plan 02), replace the existing event loop (from `for (const entry of body.entry ?? []) {` through the closing braces, INCLUDING the entire `try/catch` block around the Govi AI call) with a slim two-level loop that awaits `handleWebhookEvent(event)` for each event. Concrete:

    - Outer loop: `for (const entry of body.entry ?? []) { for (const event of entry.messaging ?? []) { await handleWebhookEvent(event); } }`
    - REMOVE: the inner `const userText: string = event.message.text;`, `console.log(\`[${senderId}] ${userText}\`);`, the `try { const { data } = await axios.post(\`${GOVI_AI_URL}/ai/chat\`, ...); await sendMessage(senderId, data.reply); } catch (...)` block — all of it. `handleWebhookEvent` owns logging now.
    - PRESERVE byte-for-byte: the `if (body.object !== "page") { res.sendStatus(404); return; }` check (existing line 30-33) and the `res.sendStatus(200);` (existing line 36).

    Update `messenger-bot/src/tests/handlers.test.ts`: remove the 4 `t.skip(...)` calls; implement the 4 test blocks. Mocking pattern: import the module namespace via `const mod = await import("../index")`, then use `t.mock.method(mod, 'sendMessage', async () => {})` (CommonJS module objects support property assignment under node:test).

    1. **CORE-01 — GET_STARTED → welcome**: After mocking `mod.sendMessage`, call `await mod.handleWebhookEvent({ postback: { payload: "GET_STARTED" }, sender: { id: "USR_1" } })`. Assert `mod.sendMessage.mock.calls.length === 1`; `mod.sendMessage.mock.calls[0].arguments[0] === "USR_1"`; `mod.sendMessage.mock.calls[0].arguments[2]` is an array of length ≥ 1 with every element `.content_type === "text"`.
    2. **CORE-03 — sendWelcomeMessage attaches quick_replies**: Mock `axios.post` instead. Call `await mod.sendWelcomeMessage("USR_1")`. Assert `axios.post.mock.calls[0].arguments[1].message.quick_replies` is an array of length 2; each entry has `content_type === "text"` AND `title.length <= 20`.
    3. **CORE-04 happy path — free text → fallback**: Mock `mod.sendMessage`. Call `await mod.handleWebhookEvent({ message: { text: "hi there" }, sender: { id: "USR_1" } })`. Assert `mod.sendMessage.mock.calls.length === 1`; arg[1] (text) includes the substring `"buttons below"`; arg[2] is the MAIN_MENU_QUICK_REPLIES array.
    4. **CORE-04 pitfall — quick_reply must NOT trigger fallback (PITFALL 6)**: Mock `mod.sendMessage`. Call `await mod.handleWebhookEvent({ message: { quick_reply: { payload: "MENU_PRODUCT_HELP" }, text: "Product Help" }, sender: { id: "USR_1" } })`. Assert `mod.sendMessage.mock.calls.length === 0`.

    Do NOT add Phase 3 routing for `MENU_PRODUCT_HELP` / `MENU_CONTACT_HUMAN` / `MENU_MAIN` — those land in Phases 3-4. They simply log + return.
  </action>
  <verify>
    <automated>cd messenger-bot &amp;&amp; npm test 2&gt;&amp;1 | tee /tmp/gsd-handlers-test.log; EC=$?; if [ $EC -ne 0 ]; then exit $EC; fi; grep -v '^#' /tmp/gsd-handlers-test.log | grep -E 'handlers\.test\.ts' | grep -qE '(ok|pass)' &amp;&amp; grep -q "export async function handleWebhookEvent" messenger-bot/src/index.ts &amp;&amp; grep -q "export async function sendWelcomeMessage" messenger-bot/src/index.ts &amp;&amp; grep -q "export async function sendFallbackMessage" messenger-bot/src/index.ts &amp;&amp; ! grep -q "/ai/chat" messenger-bot/src/index.ts &amp;&amp; awk '/if \(event.message\?\.quick_reply\)/{qr=NR} /if \(event.message\?\.text\)/{tx=NR} END{exit !(qr&gt;0 &amp;&amp; tx&gt;qr)}' messenger-bot/src/index.ts &amp;&amp; cd messenger-bot &amp;&amp; npm run build &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `messenger-bot/src/index.ts` contains all three exported functions: `sendWelcomeMessage`, `sendFallbackMessage`, `handleWebhookEvent` (grep returns 3 matches for `export async function (sendWelcomeMessage|sendFallbackMessage|handleWebhookEvent)`)
    - `messenger-bot/src/index.ts` does NOT contain the substring `/ai/chat` — the Govi AI passthrough is fully removed
    - In `messenger-bot/src/index.ts`, the line containing `if (event.message?.quick_reply)` appears BEFORE the line containing `if (event.message?.text)` (PITFALL 6 dispatch-order check enforced by `awk` in the verify command)
    - The POST `/webhook` handler body now contains the literal `await handleWebhookEvent(event)`
    - `res.sendStatus(200)` still appears BEFORE the `for (const entry ...)` loop in the POST handler (preserved verbatim)
    - The literal D-01 fallback text `"I work best with the buttons below — here's what I can help with:"` (em-dash and colon included) appears in `sendFallbackMessage`
    - `cd messenger-bot &amp;&amp; npm test` exits 0 with ALL 10 tests passing (3 HMAC + 2 sendMessage + 1 setup + 4 handlers)
    - `cd messenger-bot &amp;&amp; npm run build` exits 0 (strict TS still clean)
    - `cd messenger-bot &amp;&amp; npm run dev` boots without thrown errors when run with only `FACEBOOK_VERIFY_TOKEN` and `FACEBOOK_PAGE_ACCESS_TOKEN` set (FACEBOOK_APP_SECRET absent → graceful warning; manual smoke step in `<verification>`)
  </acceptance_criteria>
  <done>
    Event dispatcher refactored; welcome and fallback wired with quick replies; quick-reply guard prevents the PITFALL-6 double-fallback bug; AI passthrough removed; all 10 unit tests green; bot is structurally complete per the Phase 1 ROADMAP `**Goal**` line.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Facebook → bot `/webhook` (postback events) | Untrusted POST body crosses here; integrity is already proven by HMAC (Plan 02). This plan only adds dispatch logic, not new trust boundaries on the receive side |
| bot → Facebook Graph API (`me/messenger_profile`) | Outbound write that configures Facebook-side state; PAGE_ACCESS_TOKEN appears in axios `config.url` query string |
| Operator → persistent menu copy | Strings rendered in Messenger UI; controlled by us, but the copy itself is shown to end users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02-01 | Information Disclosure | `setupMessengerProfile` catch block | mitigate | Catch typed as `AxiosError`; logs only `axiosErr.message` and `axiosErr.response?.data` — never the full err (which carries `config.url` with `access_token`). Same SEC-03 pattern Plan 02 enforces on `sendMessage` |
| T-1-02-02 | Denial of Service — silent setup failure (PITFALL 5) | `setupMessengerProfile` | mitigate | On success, logs `"Messenger profile configured"`; on failure, logs the error AND continues running so the bot does not crash. Operator can detect the missing menu by absence of the success log |
| T-1-02-03 | Spoofing — postback payload forged | `handleWebhookEvent` postback branch | mitigate (upstream) | Mitigation lives in Plan 02: HMAC verification (SEC-01) gates the webhook entry, so only Facebook can deliver postback events. This dispatcher trusts the verified payload but does NOT exec, eval, or use it in a SQL query — payload is a switch-key string only |
| T-1-02-04 | Tampering — fallback text reflects user input | `sendFallbackMessage` | mitigate | Fallback text is a hardcoded constant; user input is NEVER concatenated into the outgoing message. Future phases that echo user text must re-evaluate |
| T-1-02-05 | Repudiation — quick-reply tap delivered as text falls through to fallback (PITFALL 6) | `handleWebhookEvent` dispatch order | mitigate | `event.message?.quick_reply` branch is dispatched BEFORE `event.message?.text`; unit test 4 in `handlers.test.ts` is a regression guard against future reordering |
| T-1-02-06 | DoS — handleWebhookEvent throws and crashes the for-loop | `handleWebhookEvent` (called inside POST handler) | accept (Plan 02 covers) | `sendMessage` already wraps its axios call in try/catch (Plan 02); `handleWebhookEvent` itself does no I/O that can throw beyond `sendMessage`. The outer `res.sendStatus(200)` has already fired before the loop, so even if a single event throws, Facebook has its 200 |
| T-1-02-07 | Information Disclosure — persistent menu / greeting leaks internal info | `setupMessengerProfile` payload | accept | All copy is placeholder per D-05 — generic e-commerce support phrasing; no internal IDs, no PII, no admin contact info |
</threat_model>

<verification>
- `cd messenger-bot &amp;&amp; npm test` exits 0 with all 10 tests passing
- `cd messenger-bot &amp;&amp; npm run build` exits 0 (strict TypeScript clean)
- `grep -c 'export async function ' messenger-bot/src/index.ts` returns ≥ 4 (`sendMessage`, `setupMessengerProfile`, `sendWelcomeMessage`, `sendFallbackMessage`, `handleWebhookEvent` — verify shows ≥ 4 to allow for tolerance)
- `grep -c '/ai/chat' messenger-bot/src/index.ts` returns 0
- `awk` order-check in Task 2's verify confirms quick_reply branch precedes text branch
- Manual (requires Facebook app + tunnel): set `get_started` payload "GET_STARTED" → new conversation triggers welcome with 2 quick replies
- Manual (requires Facebook app + tunnel): tap hamburger menu → see "Product Help", "Contact Human", "Main Menu"
- Manual (requires Facebook app + tunnel): type "test" → receive fallback message with the 2 quick replies; no silent dead end
- Manual: `cd messenger-bot && npm run dev` with only VERIFY_TOKEN and PAGE_ACCESS_TOKEN set (no APP_SECRET) — boot logs include `"FACEBOOK_APP_SECRET not set — skipping webhook signature verification"` AND `"Messenger profile configured"` (or the safe-logged setup failure if PAGE_ACCESS_TOKEN is invalid)
</verification>

<success_criteria>
- All 5 ROADMAP Phase 1 Success Criteria satisfied:
  1. Forged POST returns 403 — Plan 02 satisfied; this plan does not regress
  2. Get Started triggers welcome with quick-reply navigation — this plan's Task 2 (CORE-01 + CORE-03)
  3. Free text triggers re-anchor with menu options, never silent — this plan's Task 2 (CORE-04)
  4. Persistent hamburger menu shows top-level navigation at any time — this plan's Task 1 (CORE-02)
  5. Graph API send failure logged; PAGE_ACCESS_TOKEN not in logs — Plan 02 satisfied; this plan extends the same safe-log pattern to `setupMessengerProfile`
- All 10 unit tests across 4 test files green
- `npm run build` succeeds — no TypeScript errors
- `npm run dev` boots cleanly with or without `FACEBOOK_APP_SECRET`
- `messenger-bot/src/index.ts` is the only modified source file in `messenger-bot/src/` outside `tests/` (Plan 02 + Plan 03 both touch only this one file plus its tests)
- AI passthrough `/ai/chat` is no longer called by the bot — explicit retirement per SKELETON.md Out of Scope
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-bot-foundation/01-03-SUMMARY.md`
</output>
