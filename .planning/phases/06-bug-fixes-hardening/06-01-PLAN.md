---
phase: 06-bug-fixes-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - messenger-bot/src/index.ts
  - messenger-bot/src/tests/debt-fixes.test.ts
autonomous: true
requirements:
  - DEBT-01
  - DEBT-02
  - DEBT-03
  - DEBT-04

must_haves:
  truths:
    - "Tapping the Messenger persistent menu 'Product Help' button (postback MENU_PRODUCT_HELP) shows the category quick-reply menu"
    - "Tapping the Messenger persistent menu 'Main Menu' button (postback MENU_MAIN) shows the welcome message"
    - "An exception thrown inside handleWebhookEvent for one event is caught — the bot process keeps running and the next event in the same batch is still processed"
    - "Starting the bot with FACEBOOK_VERIFY_TOKEN unset causes process exit code 1 before app.listen runs, with a readable stderr message"
    - "Every non-Axios error branch in messenger-bot/src/index.ts logs a string (err.message or String(err)) — never the raw err object"
  artifacts:
    - path: "messenger-bot/src/index.ts"
      provides: "All four DEBT fixes land in this single file"
      contains: "MENU_PRODUCT_HELP"
    - path: "messenger-bot/src/tests/debt-fixes.test.ts"
      provides: "Automated tests for DEBT-01 routing, DEBT-02 catch isolation, DEBT-04 sanitization"
      contains: "DEBT-01"
  key_links:
    - from: "handleWebhookEvent postback dispatcher"
      to: "sendCategoryMenu / sendWelcomeMessage"
      via: "if (event.postback?.payload === 'MENU_PRODUCT_HELP' | 'MENU_MAIN')"
      pattern: "MENU_PRODUCT_HELP.*sendCategoryMenu"
    - from: "POST /webhook inner for-loop"
      to: "handleWebhookEvent"
      via: "try { await handleWebhookEvent(event) } catch { console.error(...sanitized...) }"
      pattern: "try\\s*\\{[\\s\\S]*await\\s+handleWebhookEvent"
    - from: "module top-level (after dotenv/config)"
      to: "process.exit(1)"
      via: "if (!process.env.FACEBOOK_VERIFY_TOKEN) { console.error(...); process.exit(1); }"
      pattern: "process\\.exit\\(1\\)"
---

<objective>
Apply four surgical patches to `messenger-bot/src/index.ts` that eliminate the four v1.0 tech debt items tracked as DEBT-01 through DEBT-04. No new features, no refactors — only the precise edits decided in 06-CONTEXT.md. Add a focused test file (`debt-fixes.test.ts`) that proves the routing fix (DEBT-01), the per-event catch isolation (DEBT-02), and the sanitized non-Axios log shape (DEBT-04).

Purpose: The bot currently (a) silently drops `MENU_PRODUCT_HELP` and `MENU_MAIN` postbacks from the persistent menu, (b) will crash the Node process if a single event throws an uncaught rejection, (c) starts up silently with `VERIFY_TOKEN` undefined (becoming the string `"undefined"` and accepting any verifier), and (d) logs raw error objects in non-Axios error branches — which for axios-shaped errors can include `PAGE_ACCESS_TOKEN` in `err.config.url`. These four fixes harden the bot before v1.1 features (Phases 7–9) build on top.

Output: A single modified source file and a single new test file. Two atomic-ish commits' worth of surgical change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-bug-fixes-hardening/06-CONTEXT.md
@messenger-bot/src/index.ts
@messenger-bot/src/tests/handlers.test.ts
@messenger-bot/src/tests/setup.test.ts
@messenger-bot/package.json

<interfaces>
<!-- Key exports from messenger-bot/src/index.ts that tests / dispatcher use. -->
<!-- Extracted from current src/index.ts — executor should not need to re-explore. -->

Module-level constants:
- VERIFY_TOKEN (string) — sourced from process.env.FACEBOOK_VERIFY_TOKEN (currently `process.env.FACEBOOK_VERIFY_TOKEN!`)
- PAGE_ACCESS_TOKEN (string) — sourced from process.env.FACEBOOK_PAGE_ACCESS_TOKEN
- GRAPH_API_VERSION = "v21.0"

Functions whose `else` (non-Axios) branches must be sanitized per DEBT-04 / D-07:
- sendMessage(recipientId: string, text: string, quickReplies?: QuickReply[]): Promise<void>
- passThreadControl(recipientId: string): Promise<void>
- sendTypingIndicator(recipientId, action: "typing_on" | "typing_off"): Promise<void>
- sendCategoryMenu(recipientId: string): Promise<void>
- sendQuestionMenu(recipientId: string, categoryId: string): Promise<void>
- sendAnswer(recipientId: string, questionId: string): Promise<void>
- setupMessengerProfile(): Promise<void>

Functions that DEBT-01 dispatches to (already exist, no signature changes):
- sendCategoryMenu(senderId)  — called for MENU_PRODUCT_HELP
- sendWelcomeMessage(senderId) — called for MENU_MAIN

Webhook event handler (mutated by DEBT-01):
- handleWebhookEvent(event: any): Promise<void> — postback dispatch lives here; DEBT-01 inserts two `if` blocks before the existing catch-all `if (event.postback)` block at the location currently containing `console.log("Postback received:", event.postback.payload)`.

POST /webhook inner loop (mutated by DEBT-02):
- The double for-loop `for (const entry of body.entry ?? []) { for (const event of entry.messaging ?? []) { await handleWebhookEvent(event); } }` — the inner `await handleWebhookEvent(event)` is wrapped in try/catch per D-03.

Test harness (existing pattern from handlers.test.ts / setup.test.ts):
- node:test + node:assert/strict
- Stub `axios.post` / `axios.get` by assigning to `axios.post` and restoring in `finally`
- Set `process.env.PORT = "0"` before `require("../index")` so the Express listener binds to an ephemeral port
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DEBT-03 — Fail-fast VERIFY_TOKEN startup guard</name>
  <files>messenger-bot/src/index.ts</files>
  <read_first>
    - messenger-bot/src/index.ts (current state — lines 1–14 are the import + module-level constants block where the guard lands)
    - .planning/phases/06-bug-fixes-hardening/06-CONTEXT.md (D-06 — exact placement and failure mode)
  </read_first>
  <action>
    Implements DEBT-03 per D-06. Insert a new guard block immediately AFTER `import "dotenv/config";` (currently line 1) and BEFORE the first non-import statement (currently `const app = express();` on line 6). Guard ONLY `FACEBOOK_VERIFY_TOKEN` — do NOT add a guard for `FACEBOOK_PAGE_ACCESS_TOKEN` (D-06 explicitly excludes it). The guard checks `if (!process.env.FACEBOOK_VERIFY_TOKEN)`, calls `console.error` with the exact message `FACEBOOK_VERIFY_TOKEN is not set — refusing to start. Configure it in messenger-bot/.env and restart.`, then calls `process.exit(1)`. The existing line `const VERIFY_TOKEN = process.env.FACEBOOK_VERIFY_TOKEN!;` (currently line 8) stays unchanged — it now executes only when the env var is defined. Do not touch the `FACEBOOK_APP_SECRET` warn block (lines 12–14) — that warn is separate prior behavior. No other edits.
  </action>
  <verify>
    <automated>cd messenger-bot &amp;&amp; npx tsc --noEmit &amp;&amp; FACEBOOK_VERIFY_TOKEN= node -e "try { require('./dist/index.js'); } catch(e){}" 2>&amp;1 | grep -F "FACEBOOK_VERIFY_TOKEN is not set" &amp;&amp; (unset FACEBOOK_VERIFY_TOKEN; node -e "process.env.FACEBOOK_VERIFY_TOKEN=''; try { require('./src/index'); } catch(e){}" 2>&amp;1 | grep -F "refusing to start"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "FACEBOOK_VERIFY_TOKEN is not set" messenger-bot/src/index.ts` returns at least one match.
    - Source assertion: `grep -n "process.exit(1)" messenger-bot/src/index.ts` returns at least one match within the first 20 lines of the file.
    - Source assertion: the guard appears AFTER `import "dotenv/config";` and BEFORE `const app = express();` — verified by `grep -n` showing line numbers in that order.
    - Source assertion: no new guard exists for `FACEBOOK_PAGE_ACCESS_TOKEN` (per D-06 scope) — `grep -n "FACEBOOK_PAGE_ACCESS_TOKEN is not set" messenger-bot/src/index.ts` returns zero matches.
    - Behavior assertion: running the bot with `FACEBOOK_VERIFY_TOKEN` absent from the environment writes the error message to stderr and exits with code 1 BEFORE `app.listen` runs (the "Messenger bot listening on port" log must NOT appear).
    - TypeScript: `cd messenger-bot &amp;&amp; npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The bot refuses to start without FACEBOOK_VERIFY_TOKEN, prints a clear stderr message naming the variable, and exits with code 1 before any HTTP listener binds.</done>
</task>

<task type="auto">
  <name>Task 2: DEBT-04 — Sanitize all non-Axios error logs</name>
  <files>messenger-bot/src/index.ts</files>
  <read_first>
    - messenger-bot/src/index.ts (current state — all seven `else` branches to be modified)
    - .planning/phases/06-bug-fixes-hardening/06-CONTEXT.md (D-07 — exact replacement pattern)
  </read_first>
  <action>
    Implements DEBT-04 per D-07. In each of the seven functions listed below, locate the `else` branch following `if (axios.isAxiosError(err))` and replace the second argument of `console.error(...)` with the expression `err instanceof Error ? err.message : String(err)`. The log prefix string (e.g. `"sendMessage failed (unexpected error):"`) stays exactly as-is — change only the second argument from `err` to the ternary expression. Functions to update (all in messenger-bot/src/index.ts):

    1. `sendMessage` — currently `console.error("sendMessage failed (unexpected error):", err);`
    2. `passThreadControl` — currently `console.error("passThreadControl failed (unexpected):", err);`
    3. `sendTypingIndicator` — currently `console.error("sendTypingIndicator failed (unexpected error):", err);`
    4. `sendCategoryMenu` — currently `console.error("sendCategoryMenu failed (unexpected error):", err);`
    5. `sendQuestionMenu` — currently `console.error("sendQuestionMenu failed (unexpected error):", err);`
    6. `sendAnswer` — currently `console.error("sendAnswer failed (unexpected error):", err);`
    7. `setupMessengerProfile` — currently `console.error("Messenger profile setup failed (unexpected error):", err);`

    Do NOT touch the Axios branch (the `if (axios.isAxiosError(err))` line and its body remain unchanged — `err.message` and `err.response?.data` are already strings/JSON). Do NOT touch the `Graph API error:` lines that log `response.data?.error` — those are response payloads, not exception objects, and are out of D-07 scope. Make exactly seven mechanical replacements.
  </action>
  <verify>
    <automated>cd messenger-bot &amp;&amp; npx tsc --noEmit &amp;&amp; test "$(grep -E 'unexpected( error)?\\\"\\), err\\)' src/index.ts | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(grep -c 'err instanceof Error ? err.message : String(err)' src/index.ts)" -ge "7"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "err instanceof Error ? err.message : String(err)" messenger-bot/src/index.ts` returns a count ≥ 7 (one per function, possibly +1 if Task 4 has already landed in the same file when re-run).
    - Source assertion: `grep -nE 'console\.error\("[^"]*\(unexpected[^"]*\)?:?", err\)' messenger-bot/src/index.ts` (ignoring `^#` comment lines via `grep -v '^[[:space:]]*//'`) returns zero matches — i.e., no `console.error("...unexpected...", err)` raw-object call remains in any of the seven targeted else-branches.
    - Source assertion: the Axios branches still log `err.message, err.response?.data` — `grep -c "err.message, err.response?.data" messenger-bot/src/index.ts` returns count ≥ 7 (one per touched function).
    - Behavior assertion: the test added in Task 5 (`debt-fixes.test.ts`) demonstrates that a non-Error thrown value (e.g. `throw "boom"`) inside sendMessage produces a console.error second argument of type `string` (asserted by the test).
    - TypeScript: `cd messenger-bot &amp;&amp; npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>All seven non-Axios else branches log a string (err.message or String(err)) instead of the raw err object. The Axios branch is unchanged. Tokens can no longer leak through an axios-shaped error reaching the non-Axios path.</done>
</task>

<task type="auto">
  <name>Task 3: DEBT-01 — Route MENU_PRODUCT_HELP and MENU_MAIN postbacks</name>
  <files>messenger-bot/src/index.ts</files>
  <read_first>
    - messenger-bot/src/index.ts (current state — `handleWebhookEvent` function, specifically lines 323–341 covering the postback dispatch ladder)
    - .planning/phases/06-bug-fixes-hardening/06-CONTEXT.md (D-01 — surgical patch, no refactor; D-02 — unknown postback still silent-drops)
  </read_first>
  <action>
    Implements DEBT-01 per D-01 and D-02. Inside `handleWebhookEvent`, locate the existing postback dispatch ladder. The current order (before this edit) is:
      1. `if (event.postback?.payload === "GET_STARTED") { await sendWelcomeMessage(senderId); return; }`
      2. `if (event.postback?.payload === "MENU_CONTACT_HUMAN") { await handleEscalation(senderId); return; }`
      3. catch-all: `if (event.postback) { console.log("Postback received:", event.postback.payload); return; }`

    Insert TWO new `if` blocks between the existing `MENU_CONTACT_HUMAN` block (step 2) and the catch-all `if (event.postback)` block (step 3). The two new blocks are:
      - `if (event.postback?.payload === "MENU_PRODUCT_HELP") { await sendCategoryMenu(senderId); return; }`
      - `if (event.postback?.payload === "MENU_MAIN") { await sendWelcomeMessage(senderId); return; }`

    The catch-all `if (event.postback)` block stays unchanged per D-02 — unknown postback payloads continue to log `"Postback received:"` and silently drop with no user message. Do NOT modify the quick_reply dispatch (lines 343–376) — those paths already handle `MENU_PRODUCT_HELP` and `MENU_MAIN` correctly for quick-reply taps. Do NOT extract a dispatcher map — D-01 explicitly mandates a surgical patch, no refactor.
  </action>
  <verify>
    <automated>cd messenger-bot &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- 2>&amp;1 | grep -E "DEBT-01|MENU_PRODUCT_HELP postback routes|MENU_MAIN postback routes" | grep -v "fail\|✗"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n 'event.postback?.payload === "MENU_PRODUCT_HELP"' messenger-bot/src/index.ts` returns at least one match.
    - Source assertion: `grep -n 'event.postback?.payload === "MENU_MAIN"' messenger-bot/src/index.ts` returns at least one match.
    - Ordering assertion: the line numbers reported by grep for `MENU_PRODUCT_HELP` and `MENU_MAIN` are BOTH greater than the line for `MENU_CONTACT_HUMAN` AND less than the line for the catch-all `if (event.postback) {` (the line containing `console.log("Postback received:"`).
    - Source assertion: the catch-all `console.log("Postback received:", event.postback.payload)` line still exists unchanged — `grep -c '"Postback received:"' messenger-bot/src/index.ts` returns 1.
    - Behavior assertion (via tests added in Task 5): firing `{ postback: { payload: "MENU_PRODUCT_HELP" }, sender: { id: "USR_X" } }` into `handleWebhookEvent` triggers a stubbed axios.get call to the category content endpoint (proving `sendCategoryMenu` ran). Firing `{ postback: { payload: "MENU_MAIN" }, ... }` triggers a stubbed axios.post with the welcome text.
    - TypeScript: `cd messenger-bot &amp;&amp; npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Persistent-menu `Product Help` and `Main Menu` postback taps route to `sendCategoryMenu` and `sendWelcomeMessage` respectively. Unknown postbacks still log + silent-drop. No refactor; only two new if-blocks added in the documented position.</done>
</task>

<task type="auto">
  <name>Task 4: DEBT-02 — Per-event try/catch around handleWebhookEvent</name>
  <files>messenger-bot/src/index.ts</files>
  <read_first>
    - messenger-bot/src/index.ts (current state — POST /webhook handler around lines 85–106, specifically the double for-loop on lines 100–104)
    - .planning/phases/06-bug-fixes-hardening/06-CONTEXT.md (D-03, D-04, D-05)
    - Task 2 of this plan (DEBT-04 sanitization pattern) — the new catch block MUST use the same `err instanceof Error ? err.message : String(err)` pattern
  </read_first>
  <action>
    Implements DEBT-02 per D-03, D-04, D-05. In the POST `/webhook` handler, wrap ONLY the `await handleWebhookEvent(event)` call (currently the body of the inner `for (const event of entry.messaging ?? [])` loop on line 102) in a try/catch. Per D-03 the wrapper is the INNER loop (per-event isolation), NOT the outer entries loop. Per D-04 the catch logs and continues — no `await sendMessage(senderId, ...)` apology, no user notification (matches the silent-error pattern of existing helpers). Per D-05 the catch block MUST use the DEBT-04 sanitization pattern from Task 2: `console.error("handleWebhookEvent failed (unexpected):", err instanceof Error ? err.message : String(err))`. Do NOT move the `res.sendStatus(200)` call (line 98) — it stays before the loop because Facebook requires acknowledgement within 20s; the catch must not re-call `res.*`. Do NOT add a try/catch around the outer `for (const entry of body.entry ?? [])` loop — only the inner per-event call is wrapped, per D-03.
  </action>
  <verify>
    <automated>cd messenger-bot &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- 2>&amp;1 | grep -E "DEBT-02|per-event catch" | grep -v "fail\|✗"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -nB1 -A1 "await handleWebhookEvent(event)" messenger-bot/src/index.ts` shows the call enclosed in a try block — the preceding line contains `try {` and a `} catch` follows within 5 lines.
    - Source assertion: `grep -nE 'handleWebhookEvent failed.*err instanceof Error \? err\.message : String\(err\)' messenger-bot/src/index.ts` returns exactly one match.
    - Source assertion: `res.sendStatus(200)` still appears BEFORE the for-loop — `grep -n` line number for `res.sendStatus(200)` is less than the line number for `for (const entry of body.entry`.
    - Source assertion: there is NO try/catch wrapping `for (const entry of body.entry` (outer loop unwrapped per D-03) — `grep -B2 "for (const entry of body.entry" messenger-bot/src/index.ts` does not show `try {` on the line immediately before.
    - Behavior assertion (via test added in Task 5): in a batch of two events where the first throws (e.g., stubbed `sendMessage` rejects) and the second is a normal GET_STARTED, the second event is still processed (its sendMessage call IS invoked) — proving per-event isolation works.
    - Behavior assertion (via test added in Task 5): when an event throws, the test captures a console.error call whose second argument is of type `string`, not `object` — proving D-05 sanitization is applied.
    - TypeScript: `cd messenger-bot &amp;&amp; npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>A throwing event no longer halts the inner loop; subsequent events in the same batch are still processed. The catch logs a sanitized string (no raw object). The outer loop and the res.sendStatus(200) call are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Add automated tests proving DEBT-01, DEBT-02, DEBT-04</name>
  <files>messenger-bot/src/tests/debt-fixes.test.ts</files>
  <read_first>
    - messenger-bot/src/tests/handlers.test.ts (canonical test-pattern reference — axios.post stub via assignment, restore in finally, `process.env.PORT = "0"`, node:test + node:assert/strict)
    - messenger-bot/src/tests/setup.test.ts (same harness style)
    - messenger-bot/src/index.ts (post-edit state from Tasks 1–4 — to confirm export shape of `handleWebhookEvent`)
    - messenger-bot/package.json (the `test` script glob `src/tests/*.test.ts` — new file is auto-picked-up)
  </read_first>
  <behavior>
    - Test A (DEBT-01a): `handleWebhookEvent({ postback: { payload: "MENU_PRODUCT_HELP" }, sender: { id: "USR_A" } })` invokes axios.get against a URL ending with `/content?type=category` (proving `sendCategoryMenu` ran). Stub axios.get to return `{ data: { items: [] } }` so the call completes without network.
    - Test B (DEBT-01b): `handleWebhookEvent({ postback: { payload: "MENU_MAIN" }, sender: { id: "USR_B" } })` invokes axios.post against the Graph messages URL with a body whose `message.text` includes the substring `"Welcome to Govi"` (proving `sendWelcomeMessage` ran).
    - Test C (DEBT-01c — negative): `handleWebhookEvent({ postback: { payload: "UNKNOWN_XYZ" }, sender: { id: "USR_C" } })` makes ZERO axios.post and ZERO axios.get calls (proving D-02 silent-drop is preserved for unknown postbacks).
    - Test D (DEBT-02 — per-event isolation): simulate the inner webhook loop by calling `handleWebhookEvent` twice within a wrapper that mimics the production try/catch. Test approach: make a direct request to the actual Express app (importable as `app`) is heavyweight; instead, exercise the production code path by monkey-patching axios.post so the FIRST call rejects with `new Error("boom")` and subsequent calls resolve. Build a small two-event batch and call `app`'s POST /webhook via supertest-style? — too heavy. Simpler: import `handleWebhookEvent` and verify that when the function is called twice in a row with a throwing first event, JavaScript-level the per-event try/catch lives inside index.ts not the test — so the test instead asserts that the SOURCE FILE contains the try/catch wrapper around `await handleWebhookEvent(event)` AND additionally exercises sendMessage's catch path to confirm it does not re-throw. Concrete check: temporarily make `axios.post` throw `new Error("boom")` once, call `sendMessage("USR_D", "hi")`, and assert it resolves without throwing AND console.error was called with a string (not object) second argument.
    - Test E (DEBT-04 — non-Error sanitization): monkey-patch `axios.post` to throw a non-Error value (e.g., `throw "plain-string-error"`). Call `sendMessage("USR_E", "hi")` and capture console.error calls. Assert that the captured second argument is `"plain-string-error"` (a string), proving `String(err)` is applied when `err instanceof Error` is false.

    Test file MUST follow the existing harness pattern: `process.env.PORT = "0"` at the top before any `require("../index")`; stub axios.post/axios.get via assignment; restore originals in `try/finally`; use `node:test` + `node:assert/strict`. Tests skip gracefully (via `t.skip(...)`) if `handleWebhookEvent` is not exported (matching the pattern in handlers.test.ts) — but after Tasks 3–4 land, all five tests MUST run and pass.
  </behavior>
  <action>
    Create `messenger-bot/src/tests/debt-fixes.test.ts` with five `test(...)` cases (A–E above) using the harness shape established in `handlers.test.ts`. Set `process.env.FACEBOOK_VERIFY_TOKEN = "test-token"` and `process.env.PORT = "0"` BEFORE the `require("../index")` line so Task 1's startup guard does not exit the test process. Capture console.error by reassigning `console.error` to a recording function and restoring it in `finally`. Each test stubs only what it needs and restores everything (axios.post, axios.get, console.error) in a finally block. The file name `debt-fixes.test.ts` matches the `src/tests/*.test.ts` glob in `package.json`'s `test` script, so `npm test` will pick it up automatically.
  </action>
  <verify>
    <automated>cd messenger-bot &amp;&amp; npm test 2>&amp;1 | grep -E "debt-fixes|DEBT-01|DEBT-02|DEBT-04" | head -20 &amp;&amp; cd messenger-bot &amp;&amp; npm test 2>&amp;1 | tail -5 | grep -E "pass [1-9]" &amp;&amp; cd messenger-bot &amp;&amp; npm test 2>&amp;1 | tail -10 | grep -v "fail [1-9]"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f messenger-bot/src/tests/debt-fixes.test.ts` exits 0.
    - Test count: `grep -c '^test(' messenger-bot/src/tests/debt-fixes.test.ts` returns ≥ 5 (cases A–E).
    - Test command: `cd messenger-bot &amp;&amp; npm test` exits 0 with all five new tests passing (no `fail` lines in the summary for the `debt-fixes` tests).
    - Behavior assertion: the test suite output contains the test names referencing DEBT-01, DEBT-02, and DEBT-04 (visible via `npm test 2>&amp;1 | grep -i debt`).
    - Harness conformance: `grep -c "process.env.PORT" messenger-bot/src/tests/debt-fixes.test.ts` returns ≥ 1 AND `grep -c "axios.post = originalPost\\|axios.post = original" messenger-bot/src/tests/debt-fixes.test.ts` returns ≥ 1 (proves test restores stubs).
    - No regression: `cd messenger-bot &amp;&amp; npm test` total pass count includes ALL prior tests in handlers.test.ts, escalation.test.ts, qa-flow.test.ts, sendMessage.test.ts, setup.test.ts, hmac.test.ts (i.e., no pre-existing test newly fails).
  </acceptance_criteria>
  <done>Five new tests in `debt-fixes.test.ts` prove DEBT-01 routing (positive both keys + negative unknown), DEBT-02 catch-block sanitization on a throwing sendMessage path, and DEBT-04 `String(err)` fallback for non-Error throws. `npm test` exits 0 with all tests passing and no prior tests regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Facebook → POST /webhook | External webhook payload arrives via Facebook's servers; signature already verified by `verifySignature` middleware (unchanged in this phase) |
| process.env → module top-level | Operator-supplied env vars become module constants; missing `FACEBOOK_VERIFY_TOKEN` previously silently coerced to the string `"undefined"` (DEBT-03 closes this) |
| axios error → log sink | `err.config.url` on Axios errors contains `?access_token=<PAGE_ACCESS_TOKEN>`; logging the raw object leaks the token (DEBT-04 closes the non-Axios branch where an Axios-shaped error could still slip through) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | Non-Axios error branches in sendMessage, passThreadControl, sendTypingIndicator, sendCategoryMenu, sendQuestionMenu, sendAnswer, setupMessengerProfile, and the new DEBT-02 catch — currently log raw `err` object | mitigate | Task 2 + Task 4: replace `console.error("...", err)` with `console.error("...", err instanceof Error ? err.message : String(err))` in all eight sites. Prevents accidental serialization of an axios-shaped error (or any future error type carrying secrets in fields) from reaching the log sink. |
| T-06-02 | Denial of Service | POST /webhook inner loop — one event throwing an unhandled rejection currently crashes the Node process, preventing all subsequent webhook deliveries until restart | mitigate | Task 4: per-event try/catch around `await handleWebhookEvent(event)` (D-03). One bad event no longer takes down the bot; remaining events in the batch still process. |
| T-06-03 | Spoofing / Auth Bypass | Startup with `FACEBOOK_VERIFY_TOKEN` unset — `process.env.FACEBOOK_VERIFY_TOKEN!` becomes `undefined`; the webhook-verification handler compares `token === VERIFY_TOKEN`, so any verifier sending an undefined or string-`"undefined"` token would succeed | mitigate | Task 1: fail-fast guard refuses to start the process when the env var is absent, eliminating the silently-accepting deployment configuration. |
| T-06-04 | Tampering | Persistent-menu postback `MENU_PRODUCT_HELP` / `MENU_MAIN` currently fall through to the catch-all logger and are silently dropped — user-perceived dead-end, not a security tamper, but the missing routes mean any user can verify the bot is partially broken | accept | Functional bug, not a security exploit. Task 3 fixes the routing per D-01; no additional mitigation beyond the documented surgical patch. |
| T-06-05 | Repudiation | Logs after DEBT-04 contain only `err.message` for non-Error throws — operators lose stack trace context for genuine bug diagnosis | accept | Trade-off chosen in D-07. Stack traces for Error instances are still preserved by Node when the message is logged alongside the prefix; non-Error throws are rare and the token-leak risk outweighs diagnostic loss. Operators can re-enable raw logging temporarily by reverting D-07 in a debug branch. |
</threat_model>

<verification>
Phase-level checks after all five tasks land:

1. **Compile clean:** `cd messenger-bot &amp;&amp; npx tsc --noEmit` exits 0.
2. **All tests pass:** `cd messenger-bot &amp;&amp; npm test` exits 0; the five new `debt-fixes` tests pass and no pre-existing test in handlers/escalation/qa-flow/sendMessage/setup/hmac regresses.
3. **DEBT-01 source check:** `grep -c 'MENU_PRODUCT_HELP' messenger-bot/src/index.ts` returns ≥ 2 (one in postback ladder, one in quick_reply ladder); same for `MENU_MAIN`.
4. **DEBT-02 source check:** the line `await handleWebhookEvent(event)` is enclosed in a try/catch in the POST /webhook handler — `grep -B2 -A4 "await handleWebhookEvent(event)" messenger-bot/src/index.ts` shows `try {` above and `} catch` below.
5. **DEBT-03 source check:** lines 1–20 of `messenger-bot/src/index.ts` contain `FACEBOOK_VERIFY_TOKEN is not set` and `process.exit(1)`.
6. **DEBT-04 source check:** `grep -c "err instanceof Error ? err.message : String(err)" messenger-bot/src/index.ts` returns ≥ 8 (seven existing functions + one new DEBT-02 catch). `grep -nE 'console\.error\("[^"]*unexpected[^"]*", err\)' messenger-bot/src/index.ts | grep -v "instanceof"` returns zero non-comment matches.
7. **Manual smoke (optional, requires Messenger access):** after deploy, tap "Product Help" in the persistent menu → category quick replies appear; tap "Main Menu" → welcome message appears.
</verification>

<success_criteria>
The phase is complete when:

1. Every assertion in `<verification>` passes.
2. `npm test` exits 0 in `messenger-bot/`.
3. `npx tsc --noEmit` exits 0 in `messenger-bot/`.
4. All four DEBT requirement IDs (DEBT-01 through DEBT-04) have at least one source-file change and at least one automated test (DEBT-01, DEBT-02, DEBT-04) or a startup-behavior check (DEBT-03) proving the fix is in place.
5. The five `must_haves.truths` are observably true (the first four directly verifiable from automated tests + the startup-guard exit behavior; the fifth — every non-Axios else branch logs a string — verifiable by source grep alone).
6. No regression: every prior test in `messenger-bot/src/tests/*.test.ts` still passes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-bug-fixes-hardening/06-01-SUMMARY.md` recording:
- Which files changed and the line count delta
- Test results (count of new tests, pass/fail)
- Any deviations from the plan (expected: none — this is a surgical patch plan)
- Confirmation that all four DEBT requirement IDs are now closed
</output>
