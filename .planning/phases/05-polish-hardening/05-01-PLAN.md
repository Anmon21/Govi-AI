---
phase: "05"
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - messenger-bot/src/tests/qa-flow.test.ts
autonomous: true
requirements:
  - POLISH-01
tags:
  - typescript
  - messenger
  - typing-indicator
  - red-state
  - tdd
mvp_mode: true

user_setup: []

must_haves:
  truths:
    - "qa-flow.test.ts asserts postCalls.length === 3 for the QUESTION:<id> success path (was 1)"
    - "qa-flow.test.ts checks the answer body at postCalls[1] (was postCalls[0])"
    - "qa-flow.test.ts contains a new test named 'qa-flow: sendAnswer typing_off fires even when API fetch fails'"
    - "The new error-path test asserts exactly 2 sender_action POSTs (typing_on + typing_off) occur in postCalls when axios.get throws"
    - "Running `cd messenger-bot && npm test` produces RED state for the typing-indicator assertions — 2 tests fail, 0 new tests pass (Wave 1 will turn them GREEN)"
  artifacts:
    - path: "messenger-bot/src/tests/qa-flow.test.ts"
      provides: "Updated assertion at line 135 + new typing_off-on-fetch-failure test"
      contains: "postCalls.length, 3"
    - path: "messenger-bot/src/tests/qa-flow.test.ts"
      provides: "New error-path test for typing_off in finally block"
      contains: "typing_off fires even when API fetch fails"
  key_links:
    - from: "messenger-bot/src/tests/qa-flow.test.ts"
      to: "messenger-bot/src/index.ts sendAnswer"
      via: "withAxiosStubs intercepts axios.post — every typing_on/off + sendMessage POST is captured"
      pattern: "sender_action"
---

## Phase Goal

**As a** customer, **I want to** see a typing indicator while the bot fetches my answer, **so that** the bot does not appear frozen during the API call.

<objective>
Wave 0 of Phase 5: drive the typing-indicator behavior to RED state by updating the existing
qa-flow QUESTION test (line 135 assertion) and adding one new test for the finally-block
guarantee (typing_off fires even when the API fetch throws). These tests will fail until
Plan 02 (Wave 1) implements `sendTypingIndicator` and wires it into `sendAnswer`.

Purpose: Establish the test contract for POLISH-01 before any implementation lands — this is
the TDD pattern used in every prior phase (01-01 / 02-01 / 04-01). Wave 1 cannot regress
typing-indicator behavior without breaking these tests.

Output: Updated qa-flow.test.ts with the 1 modified assertion + 1 new test. RED state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@.planning/phases/05-polish-hardening/05-RESEARCH.md
@.planning/phases/05-polish-hardening/05-UI-SPEC.md
@.planning/phases/05-polish-hardening/05-VALIDATION.md

@.planning/phases/03-product-q-a-flow/03-02-SUMMARY.md
@.planning/phases/04-human-escalation/04-02-SUMMARY.md

@messenger-bot/src/tests/qa-flow.test.ts
@messenger-bot/src/index.ts
@./CLAUDE.md

<interfaces>
<!-- Existing test infrastructure already in qa-flow.test.ts — no new helpers needed -->

From messenger-bot/src/tests/qa-flow.test.ts (lines 32-54):
- `withAxiosStubs({ onGet, onPost })` — replaces `axios.get` and `axios.post` with stubs;
  returns `{ getCalls: { url }[], postCalls: { url, body }[], restore() }`.
- `postCalls[i].body` shape for sendMessage: `{ recipient: { id }, message: { text, quick_replies? } }`
- `postCalls[i].body` shape for typing indicator (to be implemented in Plan 02):
  `{ recipient: { id }, sender_action: "typing_on" | "typing_off" }`
- `postCalls[i].url` is always `https://graph.facebook.com/v21.0/me/messages` for both message
  sends and typing indicator sends (Graph API uses the same endpoint with `message` vs
  `sender_action` body fields).

From messenger-bot/src/index.ts:
- `handleWebhookEvent` is the dispatcher; the QUESTION test invokes it with payload
  `QUESTION:q-shipping-01`, which routes to `sendAnswer(senderId, "q-shipping-01")`.
- `sendAnswer` currently fires exactly 1 axios.post (the sendMessage with body + main menu).
  After Wave 1 it will fire 3 (typing_on + sendMessage + typing_off).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update QUESTION test assertion for typing-indicator POSTs (postCalls.length 1 → 3)</name>
  <files>messenger-bot/src/tests/qa-flow.test.ts</files>
  <read_first>
    - messenger-bot/src/tests/qa-flow.test.ts (lines 117–141 — the QUESTION:<id> test that breaks)
    - .planning/phases/05-polish-hardening/05-RESEARCH.md (lines 264–278 — exact assertion delta)
    - .planning/phases/05-polish-hardening/05-UI-SPEC.md (lines 88–110 — call ordering rationale)
    - messenger-bot/src/index.ts (lines 262–279 — current sendAnswer shape)
  </read_first>
  <behavior>
    - The updated test still passes for the body text and quick_replies fields of the answer message
    - The updated test fails (RED) until Wave 1 lands because sendAnswer currently fires only 1 POST, not 3
    - When Wave 1 lands sendTypingIndicator + the typing_on/off wrapping, the test will pass at postCalls.length === 3 with the answer body at postCalls[1]
  </behavior>
  <action>
    In the test `qa-flow: QUESTION:<id> triggers sendAnswer with body text and main menu re-anchor` (currently at lines 117–141 of `messenger-bot/src/tests/qa-flow.test.ts`):

    1. Change `assert.strictEqual(stubs.postCalls.length, 1, "single sendMessage delivers answer + re-anchor");` to `assert.strictEqual(stubs.postCalls.length, 3, "typing_on + sendMessage + typing_off for answer delivery");`.
    2. Change `const msg = stubs.postCalls[0].body?.message;` to `const msg = stubs.postCalls[1].body?.message;`.
    3. Immediately after the count assertion, add two new assertions that lock the order:
       - assert `stubs.postCalls[0].body?.sender_action === "typing_on"` with the message "typing_on must be the first POST".
       - assert `stubs.postCalls[2].body?.sender_action === "typing_off"` with the message "typing_off must be the third POST (after sendMessage resolves)".
    4. Leave the existing `msg?.text === ANSWER_BODY` and `msg?.quick_replies` deep-equality assertions unchanged — they still hold against `postCalls[1]`.
    5. Do not change the test name. Do not change the stub setup. Do not touch the other three tests in the file (MENU_PRODUCT_HELP, CATEGORY:<id>, API error apology) — none of them call `sendAnswer`.
  </action>
  <verify>
    <automated>cd messenger-bot && npm test 2>&1 | grep -E "(qa-flow: QUESTION|fail [0-9])" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'postCalls.length, 3' messenger-bot/src/tests/qa-flow.test.ts` returns at least 1
    - Source assertion: `grep -c 'postCalls\[1\].body?.message' messenger-bot/src/tests/qa-flow.test.ts` returns at least 1
    - Source assertion: `grep -c 'sender_action === "typing_on"' messenger-bot/src/tests/qa-flow.test.ts` returns at least 1
    - Source assertion: `grep -c 'sender_action === "typing_off"' messenger-bot/src/tests/qa-flow.test.ts` returns at least 1
    - Behavior: Running `cd messenger-bot && npm test` prints `qa-flow: QUESTION:<id> triggers sendAnswer ...` as FAIL with the message "typing_on + sendMessage + typing_off for answer delivery" (RED — Wave 1 turns it GREEN)
    - Surgical: `git diff messenger-bot/src/tests/qa-flow.test.ts` touches only the QUESTION test block; the three other tests are byte-identical
  </acceptance_criteria>
  <done>
    qa-flow.test.ts has the 1→3 count update, the index 0→1 message lookup, and the two new ordering assertions. Running `npm test` produces a clear RED for the QUESTION test with the new count message visible in the failure output. No other test changes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add error-path test — typing_off fires in finally even when axios.get throws</name>
  <files>messenger-bot/src/tests/qa-flow.test.ts</files>
  <read_first>
    - messenger-bot/src/tests/qa-flow.test.ts (lines 143–165 — existing API-error apology test pattern with console.error muting)
    - .planning/phases/05-polish-hardening/05-RESEARCH.md (lines 280–303 — error-path test description)
    - .planning/phases/05-polish-hardening/05-UI-SPEC.md (lines 100–122 — Error Case Ordering + finally block contract)
  </read_first>
  <behavior>
    - When axios.get throws inside sendAnswer, postCalls contains exactly 3 entries in order: typing_on, apology sendMessage, typing_off
    - typing_off fires even though the GET threw — this is the finally-block contract from UI-SPEC
    - The apology sendMessage carries the MAIN_MENU_QUICK_REPLIES re-anchor (preserving the Phase 3 contract)
    - The test fails (RED) until Wave 1 implements the try/finally wrapping in sendAnswer
  </behavior>
  <action>
    Append a new test to the end of `messenger-bot/src/tests/qa-flow.test.ts`, AFTER the existing `qa-flow: API error sends apology + MAIN_MENU_QUICK_REPLIES (no crash)` test (which ends at line 165).

    Test name (exact string): `qa-flow: sendAnswer typing_off fires even when API fetch fails`

    Test structure (mirror the existing API-error apology test for stubs + console.error muting):
    - Skip guard: `if (!handleWebhookEvent || !MAIN_MENU_QUICK_REPLIES || !_sendCategoryMenuExported) { t.skip("pending Plan 03-02 implementation"); return; }`
    - Stub axios.get to throw `new Error("ECONNREFUSED")` for any URL containing `/content/q-shipping-01` (use the same `withAxiosStubs({ onGet: async () => { throw new Error("ECONNREFUSED"); } })` shape as the existing API-error test).
    - Mute `console.error` for the duration of the test (same try/finally restore pattern as the existing API-error test).
    - Invoke `handleWebhookEvent({ message: { quick_reply: { payload: "QUESTION:q-shipping-01" }, text: "Shipping time?" }, sender: { id: "USR_1" } })`.
    - Assertions (in order):
      1. `stubs.postCalls.length === 3` with message "typing_on + apology + typing_off when fetch fails".
      2. `stubs.postCalls[0].body?.sender_action === "typing_on"` with message "first POST must be typing_on".
      3. `stubs.postCalls[1].body?.message?.text` is a non-empty string AND `stubs.postCalls[1].body?.message?.quick_replies` deep-equals `MAIN_MENU_QUICK_REPLIES` (the apology + re-anchor).
      4. `stubs.postCalls[2].body?.sender_action === "typing_off"` with message "typing_off must fire even when fetch threw (finally block)".
    - Finally: `stubs.restore()` + restore `console.error`.

    Do not import any new symbols. Do not add new helpers. The existing `withAxiosStubs` is sufficient. Keep the same require()-and-skip dormancy pattern used throughout the file so this test skips cleanly if the module fails to load.
  </action>
  <verify>
    <automated>cd messenger-bot && grep -v '^//' messenger-bot/src/tests/qa-flow.test.ts | grep -c 'typing_off fires even when API fetch fails'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -v '^//' messenger-bot/src/tests/qa-flow.test.ts | grep -c 'typing_off fires even when API fetch fails'` returns exactly 1 (the test name appears once, not in a comment)
    - Source assertion: `grep -c 'ECONNREFUSED' messenger-bot/src/tests/qa-flow.test.ts` returns at least 2 (existing API-error test + new test)
    - Source assertion: `grep -c '"QUESTION:q-shipping-01"' messenger-bot/src/tests/qa-flow.test.ts` returns at least 2 (existing QUESTION test + new error-path test)
    - Behavior: Running `cd messenger-bot && npm test` shows the new test in the failure list with `postCalls.length` mismatch (RED — currently sendAnswer fires only 1 POST on error, not 3)
    - Test count delta: total test count rises from 21 to 22; the existing 20 unrelated tests still pass green
  </acceptance_criteria>
  <done>
    New test appended at end of file, follows the existing stub + console.error-mute pattern, asserts the 3-POST ordering for the error path, and is RED until Wave 1 implements the finally block.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → axios stubs | Tests run in-process; the stubs replace axios globally for the test scope. No real network calls. No real PAGE_ACCESS_TOKEN is read or used. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01-test-a | Information Disclosure | qa-flow.test.ts | accept | This plan touches a test file only; no PAGE_ACCESS_TOKEN is referenced or logged. Tests stub axios so no real Graph API call is made. Mitigation is in Plan 02 (Wave 1) where sendTypingIndicator is implemented with the SEC-03 isAxiosError guard. |
| T-5-01-test-b | Tampering | qa-flow.test.ts | accept | Test assertions are deterministic string equality and length checks. No external input flows through the test. |
</threat_model>

<verification>
After both tasks land, run `cd messenger-bot && npm test`. Expected state:
- Total tests: 22 (was 21; +1 new error-path test)
- Failing tests: 2 — `qa-flow: QUESTION:<id> triggers sendAnswer ...` (count is 1, expected 3) AND `qa-flow: sendAnswer typing_off fires even when API fetch fails` (count is 1, expected 3)
- Passing tests: 20 (all other tests unaffected — handlers.test.ts, escalation.test.ts, MENU_PRODUCT_HELP qa-flow tests, etc.)
- This is the expected RED state. Wave 1 (Plan 02) turns both to GREEN.
</verification>

<success_criteria>
- [ ] qa-flow.test.ts QUESTION test asserts `postCalls.length === 3` with index 1 for the message body
- [ ] qa-flow.test.ts QUESTION test asserts `postCalls[0].body.sender_action === "typing_on"` and `postCalls[2].body.sender_action === "typing_off"`
- [ ] qa-flow.test.ts contains a new test `qa-flow: sendAnswer typing_off fires even when API fetch fails`
- [ ] The new test asserts 3 POSTs in order: typing_on, apology sendMessage with main menu, typing_off
- [ ] `cd messenger-bot && npm test` shows exactly 2 failing tests (the two POLISH-01 tests in RED state)
- [ ] No other test files modified
- [ ] No production code (`messenger-bot/src/index.ts`) modified — that is Plan 02's job
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-hardening/05-01-SUMMARY.md` using the summary template.
</output>
