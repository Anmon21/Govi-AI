---
phase: 12-page-connection-api-facebook-oauth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/config.py
  - .env.example
  - app/fb_client.py
  - app/routers/pages.py
  - tests/conftest.py
  - tests/test_pages.py
autonomous: true
requirements:
  - PAGE-01
user_setup:
  - service: facebook
    why: "OAuth flow + webhook subscription require a Facebook App registered in Meta App Dashboard. App Review for pages_messaging Advanced Access is on the critical path — submit during Phase 12 execution."
    env_vars:
      - name: FACEBOOK_APP_ID
        source: "Meta App Dashboard -> Settings -> Basic -> App ID"
      - name: FACEBOOK_APP_SECRET
        source: "Meta App Dashboard -> Settings -> Basic -> App Secret"
      - name: FACEBOOK_REDIRECT_URI
        source: "Must EXACTLY match the value registered under Facebook Login -> Settings -> Valid OAuth Redirect URIs"
    dashboard_config:
      - task: "Register Valid OAuth Redirect URI matching FACEBOOK_REDIRECT_URI character-for-character (Pitfall 1)"
        location: "Meta App Dashboard -> Products -> Facebook Login -> Settings -> Valid OAuth Redirect URIs"
      - task: "Submit pages_messaging permission for Advanced Access (App Review). Required for Live mode message delivery. Days-to-weeks turnaround."
        location: "Meta App Dashboard -> App Review -> Permissions and Features"
      - task: "Add test users (developer/tester role) so non-admin Facebook accounts can complete OAuth in Development mode (Pitfall 5)"
        location: "Meta App Dashboard -> Roles -> Testers"

must_haves:
  truths:
    - "Settings exposes fb_app_id, fb_app_secret, fb_redirect_uri fields, each defaulting to empty string"
    - ".env.example documents FACEBOOK_APP_ID, FACEBOOK_APP_SECRET, FACEBOOK_REDIRECT_URI"
    - "app/fb_client.py exports exchange_code_for_short_token, exchange_for_long_lived_token, get_user_pages, subscribe_page_webhook, unsubscribe_page_webhook, check_token_health"
    - "app/fb_client.py uses GRAPH_BASE = 'https://graph.facebook.com/v25.0' for every Graph API URL"
    - "app/fb_client.py guards against missing fb_app_id or fb_app_secret with RuntimeError (matching app/crypto.py _fernet pattern)"
    - "app/fb_client.py functions that exchange tokens call resp.raise_for_status() AND check for 'error' key in JSON (Pitfall 2)"
    - "tests/conftest.py db_client fixture monkeypatches fernet_key, fb_app_id, fb_app_secret, fb_redirect_uri"
    - "app/routers/pages.py defines router = APIRouter(tags=['pages']) with NO prefix"
    - "GET /auth/facebook/start requires get_current_tenant Depends and returns RedirectResponse (302) to https://www.facebook.com/v25.0/dialog/oauth with client_id, redirect_uri, scope, state, response_type=code query params"
    - "State JWT is HS256-signed with settings.jwt_secret, contains tenant_id + nonce + exp (10-minute expiry)"
    - "GET /auth/facebook/callback verifies state JWT BEFORE any Graph API call; expired or invalid state returns HTTP 400"
    - "Callback performs 3-step exchange (code -> short -> long -> pages), then for each page: encrypts the page access_token, UPSERTs into pages table, subscribes webhook — all inside one conn try/except with conn.rollback() on subscription failure"
    - "tests/test_pages.py contains 6 PAGE-01 test functions: test_oauth_start, test_state_jwt, test_callback_invalid_state, test_callback_expired_state, test_callback_webhook_failure_rollback, test_callback_success — all passing"
  artifacts:
    - path: "app/config.py"
      provides: "fb_app_id, fb_app_secret, fb_redirect_uri fields on Settings"
      contains: "fb_app_id"
    - path: ".env.example"
      provides: "FACEBOOK_APP_ID, FACEBOOK_APP_SECRET, FACEBOOK_REDIRECT_URI placeholders"
      contains: "FACEBOOK_APP_ID="
    - path: "app/fb_client.py"
      provides: "Thin Graph API helper functions for token exchange, webhook subscription, debug_token health check"
      exports:
        - "GRAPH_BASE"
        - "exchange_code_for_short_token"
        - "exchange_for_long_lived_token"
        - "get_user_pages"
        - "subscribe_page_webhook"
        - "unsubscribe_page_webhook"
        - "check_token_health"
      min_lines: 60
    - path: "app/routers/pages.py"
      provides: "OAuth start + callback endpoints + create_oauth_state/verify_oauth_state helpers + _store_pages_and_subscribe transaction"
      exports:
        - "router"
        - "create_oauth_state"
        - "verify_oauth_state"
      min_lines: 80
    - path: "tests/conftest.py"
      provides: "db_client fixture extended with fernet_key, fb_app_id, fb_app_secret, fb_redirect_uri monkeypatches"
      contains: "fb_app_id"
    - path: "tests/test_pages.py"
      provides: "6 PAGE-01 test functions + MockResponse helper class + _connect_page helper"
      contains: "def test_oauth_start"
  key_links:
    - from: "app/routers/pages.py create_oauth_state"
      to: "app.config.settings.jwt_secret"
      via: "jwt.encode(payload, settings.jwt_secret, algorithm='HS256')"
      pattern: "jwt\\.encode.*settings\\.jwt_secret"
    - from: "app/routers/pages.py callback handler"
      to: "app.crypto.encrypt_token"
      via: "encrypt_token(page['access_token']) before INSERT/UPDATE on pages.access_token_enc"
      pattern: "encrypt_token"
    - from: "app/routers/pages.py _store_pages_and_subscribe"
      to: "app.fb_client.subscribe_page_webhook"
      via: "called inside conn try block; raises propagate to except -> conn.rollback()"
      pattern: "subscribe_page_webhook|conn\\.rollback"
    - from: "app/fb_client.py"
      to: "https://graph.facebook.com/v25.0"
      via: "GRAPH_BASE module constant used for every httpx.get/post/delete URL"
      pattern: "GRAPH_BASE.*=.*v25\\.0"
---

<objective>
Implement the Facebook OAuth flow end-to-end: configuration plumbing, Graph API helper module, OAuth start + callback endpoints with CSRF-protected state, full 3-step token exchange, Fernet-encrypted Page token storage, atomic webhook subscription, and a complete PAGE-01 test suite covering the success path plus four failure modes (invalid state, expired state, webhook rollback, no pages).

Purpose: PAGE-01 is the gating requirement for Phase 12. Without a working callback, none of PAGE-02/03/04 can be tested with real data. Building the helpers (fb_client) + the wiring (pages router) + the tests in one plan keeps the OAuth flow surgically scoped and verifiable in isolation.

Output: 6 files created or modified (3 new, 3 modified). Six PAGE-01 tests in tests/test_pages.py pass. The full suite (37 existing + 6 new = 43) stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-page-connection-api-facebook-oauth/12-RESEARCH.md
@.planning/phases/12-page-connection-api-facebook-oauth/12-PATTERNS.md
@.planning/phases/12-page-connection-api-facebook-oauth/12-VALIDATION.md

<interfaces>
<!-- Existing identifiers the executor MUST reuse unchanged. Do not redefine. -->

From app/config.py (Settings class — extend, do not replace):
```
class Settings(BaseSettings):
    anthropic_api_key: str = ""
    app_env: str = "development"
    app_port: int = 8000
    vault_path: str = ""
    db_path: str = "govi.db"
    fernet_key: str = ""
    jwt_secret: str = ""
    super_admin_email: str = ""
    super_admin_password: str = ""

    model_config = {"env_file": ".env"}

settings = Settings()
```

From app/auth.py (existing — reuse):
```
oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/auth/login")
async def get_current_tenant(token: str = Depends(oauth2_scheme)) -> dict
```
get_current_tenant returns a dict with claim keys: sub (tenant_id as STRING), role, exp.

From app/crypto.py (existing — reuse):
```
def encrypt_token(plain: str) -> str
def decrypt_token(enc: str) -> str  # raises ValueError on InvalidToken
```

From app/db.py (existing — reuse):
```
def get_connection(db_path: str) -> sqlite3.Connection  # busy_timeout=5000, foreign_keys=ON, row_factory=Row
```

From app/db.py pages table schema (existing — DO NOT MODIFY):
```
CREATE TABLE pages (
    id               INTEGER PRIMARY KEY AUTOINCREMENT,
    tenant_id        INTEGER NOT NULL REFERENCES tenants(id),
    page_fb_id       TEXT    NOT NULL UNIQUE,
    page_name        TEXT    NOT NULL,
    access_token_enc TEXT,
    is_active        INTEGER NOT NULL DEFAULT 1,
    created_at       TEXT    NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%SZ', 'now'))
);
```
Column name is `page_fb_id` (NOT `page_id`). Encrypted token column is `access_token_enc` (NOT `access_token`).

From tests/conftest.py db_client fixture (existing — extend):
```
@pytest.fixture
def db_client(tmp_path, monkeypatch):
    db_path = str(tmp_path / "test.db")
    monkeypatch.setattr(config_module.settings, "db_path", db_path)
    monkeypatch.setattr(config_module.settings, "jwt_secret", "test-secret-do-not-use-in-prod")
    init_schema(db_path)
    # ... seeds super-admin row ...
    yield SimpleNamespace(client, super_admin_id, super_admin_email, super_admin_password, db_path)
```

From tests/test_auth.py (existing — analog for helper style):
```
def _login(client, email, password) -> str  # returns access_token
def _auth_headers(token: str) -> dict       # {"Authorization": f"Bearer {token}"}
def _create_client(client, admin_token, email, password) -> dict  # POST /admin/tenants
```

From app/routers/tenants.py (analog — copy try/finally + ? placeholder pattern; reuse Pydantic BaseModel inline style).

Graph API constant — every Graph API URL in app/fb_client.py MUST be built from:
```
GRAPH_BASE = "https://graph.facebook.com/v25.0"
```
Endpoints used in this plan: GRAPH_BASE + "/oauth/access_token", GRAPH_BASE + "/me/accounts", GRAPH_BASE + f"/{page_fb_id}/subscribed_apps".

OAuth scope string (exact value sent to Facebook dialog):
```
pages_show_list,pages_manage_metadata,pages_messaging
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config + env + conftest extensions + fb_client.py helper module</name>
  <files>app/config.py, .env.example, tests/conftest.py, app/fb_client.py</files>
  <read_first>
    - app/config.py (current Settings — extend, do not rewrite)
    - .env.example (current keys — append, do not rewrite)
    - tests/conftest.py (db_client fixture — extend monkeypatch block)
    - app/crypto.py (guard pattern — _fernet() RuntimeError reference)
    - .planning/phases/12-page-connection-api-facebook-oauth/12-RESEARCH.md (Pattern 2, Pattern 3, Pattern 4, Pitfall 2)
    - .planning/phases/12-page-connection-api-facebook-oauth/12-PATTERNS.md (fb_client analog section + Settings extension section)
  </read_first>
  <action>
    Step 1 — app/config.py: Append three fields to the Settings class, placed AFTER `super_admin_password: str = ""` and BEFORE `model_config = {"env_file": ".env"}`. Field names and defaults (verbatim): `fb_app_id: str = ""`, `fb_app_secret: str = ""`, `fb_redirect_uri: str = ""`. Do not modify any existing field, the singleton instantiation, or model_config.

    Step 2 — .env.example: Append three lines after the existing SUPER_ADMIN_PASSWORD line, in this exact order: `FACEBOOK_APP_ID=`, `FACEBOOK_APP_SECRET=`, `FACEBOOK_REDIRECT_URI=https://yourapi.example.com/auth/facebook/callback`. Add a single comment line above them: `# Facebook OAuth — must match Meta App Dashboard registration exactly`. Do not modify any existing line.

    Step 3 — tests/conftest.py: Inside the `db_client` fixture, add four `monkeypatch.setattr(config_module.settings, ...)` lines immediately after the existing `jwt_secret` monkeypatch line and BEFORE `init_schema(db_path)`:
    - fernet_key = Fernet.generate_key().decode() (requires `from cryptography.fernet import Fernet` at top of conftest.py)
    - fb_app_id = "test-app-id"
    - fb_app_secret = "test-app-secret"
    - fb_redirect_uri = "http://localhost:8000/auth/facebook/callback"
    Add `from cryptography.fernet import Fernet` import at the top alongside existing imports. Do not change the existing fixture signature, super-admin seeding, or SimpleNamespace yield shape.

    Step 4 — app/fb_client.py (new file): Pure helper module following the app/crypto.py shape (no FastAPI types beyond HTTPException). Imports: `import httpx`, `from fastapi import HTTPException`, `from app.config import settings`. Module-level constant: `GRAPH_BASE = "https://graph.facebook.com/v25.0"`. Define six functions, each annotated with return types:

    - `exchange_code_for_short_token(code: str) -> str` — GET to GRAPH_BASE + "/oauth/access_token" with params client_id, redirect_uri, client_secret, code. Guard at function entry: raise RuntimeError("FACEBOOK_APP_ID / FACEBOOK_APP_SECRET / FACEBOOK_REDIRECT_URI not configured.") if any of settings.fb_app_id, settings.fb_app_secret, or settings.fb_redirect_uri is empty. After httpx call: resp.raise_for_status(), then `data = resp.json()`, then `if "error" in data: raise HTTPException(status_code=400, detail=data["error"]["message"])`. Return `data["access_token"]`.

    - `exchange_for_long_lived_token(short_token: str) -> str` — GET to GRAPH_BASE + "/oauth/access_token" with params grant_type="fb_exchange_token", client_id, client_secret, fb_exchange_token=short_token. Same guard + raise_for_status + error-key check pattern. Return `data["access_token"]`.

    - `get_user_pages(long_lived_token: str) -> list[dict]` — GET to GRAPH_BASE + "/me/accounts" with param access_token=long_lived_token. Same guard + raise_for_status + error-key check pattern. Return `data.get("data", [])`. Each list item has keys id, name, access_token (the Page access token).

    - `subscribe_page_webhook(page_fb_id: str, page_access_token: str) -> None` — POST to GRAPH_BASE + f"/{page_fb_id}/subscribed_apps" with params subscribed_fields="messages,messaging_postbacks,messaging_referrals" and access_token=page_access_token. raise_for_status + error-key check. Returns None.

    - `unsubscribe_page_webhook(page_fb_id: str, page_access_token: str) -> None` — DELETE to GRAPH_BASE + f"/{page_fb_id}/subscribed_apps" with params access_token=page_access_token. Wrap entire body in try/except Exception: pass (best-effort per Pitfall — disconnect should not fail on Graph API errors). Returns None.

    - `check_token_health(page_access_token: str) -> bool` — Construct app_token via inline string concatenation: `app_token = f"{settings.fb_app_id}|{settings.fb_app_secret}"`. GET to GRAPH_BASE + "/debug_token" with params input_token=page_access_token, access_token=app_token. Wrap in try/except Exception: return False. On success: `data = resp.json().get("data", {})`, return `bool(data.get("is_valid", False))`.

    Use `httpx.get(url, params={...})`, `httpx.post(url, params={...})`, `httpx.delete(url, params={...})` (synchronous, per Pitfall 8 — async is deferred). Never log secrets. Do not print, do not include settings.fb_app_secret in any returned exception detail.
  </action>
  <verify>
    <automated>python3 -c "from app.config import settings; assert hasattr(settings, 'fb_app_id') and hasattr(settings, 'fb_app_secret') and hasattr(settings, 'fb_redirect_uri'), 'missing fields'" &amp;&amp; python3 -c "from app import fb_client; assert fb_client.GRAPH_BASE == 'https://graph.facebook.com/v25.0'; assert all(hasattr(fb_client, n) for n in ['exchange_code_for_short_token','exchange_for_long_lived_token','get_user_pages','subscribe_page_webhook','unsubscribe_page_webhook','check_token_health'])" &amp;&amp; python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'fb_app_id: str = ""' app/config.py` returns 1
    - Source assertion: `grep -c 'fb_app_secret: str = ""' app/config.py` returns 1
    - Source assertion: `grep -c 'fb_redirect_uri: str = ""' app/config.py` returns 1
    - Source assertion: `grep -c '^FACEBOOK_APP_ID=' .env.example` returns 1
    - Source assertion: `grep -c '^FACEBOOK_APP_SECRET=' .env.example` returns 1
    - Source assertion: `grep -c '^FACEBOOK_REDIRECT_URI=' .env.example` returns 1
    - Source assertion: `grep -c 'GRAPH_BASE = "https://graph.facebook.com/v25.0"' app/fb_client.py` returns 1
    - Source assertion: `grep -c 'fb_app_id' tests/conftest.py` is >= 1
    - Source assertion: `grep -c 'from cryptography.fernet import Fernet' tests/conftest.py` is >= 1
    - Behavior: `python3 -c "from app import fb_client"` exits 0
    - Behavior: `python3 -m pytest tests/ -q` reports 37 passed (existing suite still green; no new tests yet)
    - Source assertion: `grep -n 'app_secret' app/fb_client.py | grep -v '^#'` shows fb_app_secret used ONLY inside function bodies (never in module-level prints, logs, or default exception detail strings)
  </acceptance_criteria>
  <done>
    Settings has three new fb_* fields. .env.example documents three new FACEBOOK_* keys. tests/conftest.py db_client fixture monkeypatches all four (fernet_key + 3 fb_*). app/fb_client.py exists with GRAPH_BASE constant and six exported functions, all using v25.0 URLs, all checking for error JSON after raise_for_status (except check_token_health and unsubscribe which swallow). Full pytest suite is still green at 37 passing.
  </done>
</task>

<task type="auto">
  <name>Task 2: app/routers/pages.py with start + callback endpoints (PAGE-01 wiring)</name>
  <files>app/routers/pages.py, app/main.py</files>
  <read_first>
    - app/routers/tenants.py (router + try/finally DB pattern — copy structure)
    - app/auth.py (get_current_tenant Depends + JWT encode/decode pattern in create_access_token/verify_token)
    - app/db.py (pages table schema — column names: page_fb_id, access_token_enc, is_active)
    - app/crypto.py (encrypt_token signature)
    - app/fb_client.py (functions created in Task 1 — exchange_code_for_short_token, exchange_for_long_lived_token, get_user_pages, subscribe_page_webhook)
    - app/main.py (router mount block — line 30-34)
    - .planning/phases/12-page-connection-api-facebook-oauth/12-RESEARCH.md (Pattern 1, Pattern 2, Pattern 5, Callback Endpoint Skeleton, State Parameter section)
    - .planning/phases/12-page-connection-api-facebook-oauth/12-PATTERNS.md (router declaration + cross-tenant isolation patterns)
  </read_first>
  <action>
    Step 1 — app/routers/pages.py (new file):

    Imports: `import secrets`, `import jwt`, `from datetime import datetime, timedelta, timezone`, `from urllib.parse import urlencode`, `from fastapi import APIRouter, Depends, HTTPException`, `from fastapi.responses import RedirectResponse`, `from pydantic import BaseModel`, `from typing import Literal`, `from app.auth import get_current_tenant`, `from app.config import settings`, `from app.crypto import encrypt_token`, `from app.db import get_connection`, `from app import fb_client`.

    Router declaration (no prefix, matches tenants.py): `router = APIRouter(tags=["pages"])`.

    Define two module-level helper functions (NOT route handlers):

    - `create_oauth_state(tenant_id: str) -> str`: Build payload dict with keys `tenant_id` (string), `nonce` (secrets.token_hex(16)), `exp` (datetime.now(tz=timezone.utc) + timedelta(minutes=10)). Guard: if not settings.jwt_secret, raise RuntimeError("JWT_SECRET is not configured. Set it in .env."). Return jwt.encode(payload, settings.jwt_secret, algorithm="HS256").

    - `verify_oauth_state(state: str) -> str`: try jwt.decode(state, settings.jwt_secret, algorithms=["HS256"]) then return decoded["tenant_id"]. Except jwt.ExpiredSignatureError: raise HTTPException(status_code=400, detail="OAuth state expired — please try again"). Except jwt.InvalidTokenError: raise HTTPException(status_code=400, detail="Invalid OAuth state").

    Define one private transaction helper:

    - `_store_pages_and_subscribe(pages: list[dict], tenant_id: int) -> int`: Open conn via get_connection(settings.db_path). Wrap entire body in try/except/finally. Inside try: iterate `pages`; for each page extract page_fb_id=page["id"], page_name=page["name"], page_token=page["access_token"]; compute encrypted=encrypt_token(page_token); SELECT id FROM pages WHERE page_fb_id=? AND tenant_id=? to detect existing row; if found execute UPDATE pages SET access_token_enc=?, page_name=?, is_active=1 WHERE page_fb_id=? AND tenant_id=?; else execute INSERT INTO pages (tenant_id, page_fb_id, page_name, access_token_enc) VALUES (?, ?, ?, ?); then call fb_client.subscribe_page_webhook(page_fb_id, page_token); increment stored counter. After loop: conn.commit(). On any Exception in try: conn.rollback(), then `raise`. In finally: conn.close(). Return stored count. All SQL parameter binding uses ? placeholders, never f-strings.

    Define two route handlers:

    - `@router.get("/auth/facebook/start")` async def facebook_oauth_start(current: dict = Depends(get_current_tenant)) -> RedirectResponse:
        tenant_id = current["sub"] (already string per Phase 11 JWT contract)
        state = create_oauth_state(tenant_id)
        params dict = {"client_id": settings.fb_app_id, "redirect_uri": settings.fb_redirect_uri, "scope": "pages_show_list,pages_manage_metadata,pages_messaging", "state": state, "response_type": "code"}
        url = f"https://www.facebook.com/v25.0/dialog/oauth?{urlencode(params)}"
        return RedirectResponse(url=url, status_code=302)

    - `@router.get("/auth/facebook/callback")` async def facebook_oauth_callback(code: str, state: str) -> dict:
        tenant_id_str = verify_oauth_state(state)  # raises HTTPException(400) on bad/expired state — happens BEFORE any Graph API call
        short_token = fb_client.exchange_code_for_short_token(code)
        long_token = fb_client.exchange_for_long_lived_token(short_token)
        pages_data = fb_client.get_user_pages(long_token)
        if not pages_data: raise HTTPException(status_code=400, detail="No Facebook Pages found for this account")
        count = _store_pages_and_subscribe(pages_data, int(tenant_id_str))
        return {"pages_connected": count}

    The callback handler does NOT use get_current_tenant — auth is carried by the state JWT (per RESEARCH.md Pattern 1 + verify_oauth_state).

    Step 2 — app/main.py: Modify two lines only.
    - Change `from app.routers import health, ai, content, auth, tenants` (line 9) to `from app.routers import health, ai, content, auth, tenants, pages`.
    - After the line `app.include_router(tenants.router)` (line 34), append a single new line: `app.include_router(pages.router)`.
    Do not modify the lifespan, the CORS block, the FastAPI() constructor, the /landing route, or any other line.
  </action>
  <verify>
    <automated>python3 -c "from app.routers import pages; assert hasattr(pages, 'router') and hasattr(pages, 'create_oauth_state') and hasattr(pages, 'verify_oauth_state')" &amp;&amp; python3 -c "from app.main import app; routes = {r.path for r in app.routes}; assert '/auth/facebook/start' in routes and '/auth/facebook/callback' in routes, f'missing routes: {routes}'" &amp;&amp; python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'router = APIRouter(tags=\["pages"\])' app/routers/pages.py` returns 1
    - Source assertion: `grep -c '@router.get("/auth/facebook/start")' app/routers/pages.py` returns 1
    - Source assertion: `grep -c '@router.get("/auth/facebook/callback")' app/routers/pages.py` returns 1
    - Source assertion: `grep -c 'def create_oauth_state' app/routers/pages.py` returns 1
    - Source assertion: `grep -c 'def verify_oauth_state' app/routers/pages.py` returns 1
    - Source assertion: `grep -c 'def _store_pages_and_subscribe' app/routers/pages.py` returns 1
    - Source assertion: `grep -c 'timedelta(minutes=10)' app/routers/pages.py` returns 1
    - Source assertion: `grep -c 'encrypt_token' app/routers/pages.py` is >= 1
    - Source assertion: `grep -c 'conn.rollback' app/routers/pages.py` is >= 1
    - Source assertion: `grep -c 'pages_show_list,pages_manage_metadata,pages_messaging' app/routers/pages.py` returns 1
    - Source assertion: `grep -c 'app.include_router(pages.router)' app/main.py` returns 1
    - Source assertion: `grep -c 'from app.routers import health, ai, content, auth, tenants, pages' app/main.py` returns 1
    - Source assertion: `grep -E "f-?'.*WHERE.*\{" app/routers/pages.py` returns no matches (no f-strings inside SQL — must use ? placeholders)
    - Behavior: `python3 -c "from app.main import app; from starlette.routing import Route; paths={getattr(r,'path',None) for r in app.routes}; assert '/auth/facebook/start' in paths and '/auth/facebook/callback' in paths"` exits 0
    - Behavior: `python3 -m pytest tests/ -q` still reports 37 passed (no new tests yet; existing suite unbroken)
  </acceptance_criteria>
  <done>
    app/routers/pages.py exists with the two OAuth route handlers plus three helpers (create_oauth_state, verify_oauth_state, _store_pages_and_subscribe). app/main.py includes pages.router. The two new routes are reachable via FastAPI's route table. Existing 37 tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: PAGE-01 test suite — 6 tests with mocked Graph API</name>
  <files>tests/test_pages.py</files>
  <read_first>
    - tests/test_auth.py (helper patterns: _login, _auth_headers, _create_client; assertion style; SimpleNamespace fixture usage)
    - tests/conftest.py (db_client fixture shape after Task 1 extension)
    - app/routers/pages.py (the code under test — function signatures, state JWT shape, error details, success response shape)
    - app/fb_client.py (functions to monkeypatch by full module path)
    - app/crypto.py (encrypt_token — tests will call it directly to set up pre-existing rows for the UPSERT path test)
    - app/db.py (pages table column names — page_fb_id, access_token_enc, is_active)
    - .planning/phases/12-page-connection-api-facebook-oauth/12-RESEARCH.md (Validation Architecture — test IDs and required behaviors per PAGE-01)
    - .planning/phases/12-page-connection-api-facebook-oauth/12-PATTERNS.md (httpx mock pattern via monkeypatch.setattr on "app.fb_client.httpx.get")
    - .planning/phases/12-page-connection-api-facebook-oauth/12-VALIDATION.md (Per-Task Verification Map — test names)
  </read_first>
  <action>
    Create tests/test_pages.py with module-level imports: `import jwt`, `from datetime import datetime, timedelta, timezone`, `import pytest`, `import httpx`, `from app.config import settings`, `from app.db import get_connection`, `from app.crypto import encrypt_token`, plus import the helpers `_login`, `_auth_headers` from `tests.test_auth` (or redefine inline if cross-file import is problematic — choose whichever loads cleanly under pytest).

    Define a `MockResponse` class at module scope:
    - `__init__(self, json_data: dict, status_code: int = 200)` stores both
    - `raise_for_status(self)` — if self.status_code >= 400, raise `httpx.HTTPStatusError("error", request=None, response=self)`; else return None
    - `json(self)` returns self._json

    Define a helper `_make_client_token(db_client)` that creates a client tenant via the super-admin route and returns `(client_id, client_token)`.

    Implement exactly 6 PAGE-01 tests, each function name MUST match the verification map:

    1. `def test_oauth_start(db_client, monkeypatch)`:
       - Login as super-admin (or as a client tenant — both must be valid since get_current_tenant only checks signature, not role).
       - GET /auth/facebook/start with Bearer header AND follow_redirects=False (use TestClient's `client.get(..., follow_redirects=False)`).
       - Assert status_code == 302.
       - Parse the `location` header. Assert it startswith("https://www.facebook.com/v25.0/dialog/oauth?").
       - Assert it contains `client_id=test-app-id`, `redirect_uri=http%3A%2F%2Flocalhost%3A8000%2Fauth%2Ffacebook%2Fcallback` (url-encoded), `scope=pages_show_list%2Cpages_manage_metadata%2Cpages_messaging`, `response_type=code`, and `state=`.
       - Without a Bearer header: GET /auth/facebook/start returns 401.

    2. `def test_state_jwt(db_client)`:
       - Login, GET /auth/facebook/start, extract the `state` query param from the location header (use urllib.parse.parse_qs).
       - Decode it with jwt.decode(state, settings.jwt_secret, algorithms=["HS256"]).
       - Assert decoded["tenant_id"] equals the calling tenant's id (as string).
       - Assert "nonce" key exists and len(decoded["nonce"]) >= 16.
       - Assert "exp" key exists and decoded["exp"] > int(datetime.now(tz=timezone.utc).timestamp()) and decoded["exp"] <= int((datetime.now(tz=timezone.utc) + timedelta(minutes=11)).timestamp()).

    3. `def test_callback_invalid_state(db_client)`:
       - GET /auth/facebook/callback?code=anycode&state=not.a.valid.jwt
       - Assert status_code == 400 and response.json()["detail"] in ("Invalid OAuth state", "OAuth state expired — please try again") — accept either since a malformed token raises InvalidTokenError.

    4. `def test_callback_expired_state(db_client)`:
       - Manually build a state JWT with exp set to 1 minute in the past: jwt.encode({"tenant_id": "1", "nonce": "abc", "exp": datetime.now(tz=timezone.utc) - timedelta(minutes=1)}, settings.jwt_secret, algorithm="HS256").
       - GET /auth/facebook/callback?code=anycode&state=<expired_state>
       - Assert status_code == 400.
       - Assert response.json()["detail"] == "OAuth state expired — please try again".

    5. `def test_callback_webhook_failure_rollback(db_client, monkeypatch)`:
       - Create a client tenant, login, capture client_id.
       - Build a valid state JWT for that tenant.
       - Monkeypatch the three module-level httpx calls inside app.fb_client to simulate the three GET steps returning success, AND monkeypatch `app.fb_client.subscribe_page_webhook` to raise `RuntimeError("subscription failed")`.
         - `monkeypatch.setattr("app.fb_client.httpx.get", lambda url, **kw: ...)` — return a MockResponse appropriate to which URL was called. Use a dispatcher: if "/oauth/access_token" in url AND "fb_exchange_token" in kw["params"] → long-lived branch returning {"access_token": "long-tok"}; elif "/oauth/access_token" in url → short branch returning {"access_token": "short-tok"}; elif "/me/accounts" in url → return {"data": [{"id": "fb-page-1", "name": "Test Page", "access_token": "page-tok"}]}.
         - `monkeypatch.setattr("app.fb_client.subscribe_page_webhook", lambda page_fb_id, page_access_token: (_ for _ in ()).throw(RuntimeError("subscription failed")))` — or use a def stub.
       - GET /auth/facebook/callback?code=test&state=<valid_state>
       - Assert status_code in (500, 502, 400) — exact code depends on whether FastAPI auto-converts RuntimeError; the contract here is that the request fails AND no row is committed.
       - Then assert direct SQL: `SELECT COUNT(*) FROM pages WHERE tenant_id = ?` returns 0 (no committed row). This proves conn.rollback() executed before exception propagation.

    6. `def test_callback_success(db_client, monkeypatch)`:
       - Create a client tenant, build a valid state JWT for that tenant.
       - Monkeypatch the same three GET endpoints to return the success payload (short token, long token, one page) per the dispatcher above.
       - Monkeypatch `app.fb_client.httpx.post` to return MockResponse({"success": True}) so subscribe_page_webhook succeeds.
       - GET /auth/facebook/callback?code=test&state=<valid_state>
       - Assert status_code == 200 and response.json() == {"pages_connected": 1}.
       - Direct SQL: SELECT page_fb_id, page_name, access_token_enc, is_active FROM pages WHERE tenant_id = ? — assert exactly 1 row, page_fb_id == "fb-page-1", page_name == "Test Page", is_active == 1, and access_token_enc is non-null AND is NOT the literal string "page-tok" (confirms Fernet encryption was applied). Decrypt via app.crypto.decrypt_token(row["access_token_enc"]) and assert it equals "page-tok".
       - Re-running the same callback (UPSERT path) must still return {"pages_connected": 1} and produce exactly 1 row, not 2.

    All assertions use direct comparison (`==`) or `pytest.fail` on mismatch — no soft assertions. Test functions must each call `client = db_client.client`. Do not use unittest.mock.patch (project convention is monkeypatch).
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_pages.py -k "test_oauth_start or test_state_jwt or test_callback_invalid_state or test_callback_expired_state or test_callback_webhook_failure_rollback or test_callback_success" -x -q &amp;&amp; python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'def test_oauth_start' tests/test_pages.py` returns 1
    - Source assertion: `grep -c 'def test_state_jwt' tests/test_pages.py` returns 1
    - Source assertion: `grep -c 'def test_callback_invalid_state' tests/test_pages.py` returns 1
    - Source assertion: `grep -c 'def test_callback_expired_state' tests/test_pages.py` returns 1
    - Source assertion: `grep -c 'def test_callback_webhook_failure_rollback' tests/test_pages.py` returns 1
    - Source assertion: `grep -c 'def test_callback_success' tests/test_pages.py` returns 1
    - Source assertion: `grep -c 'class MockResponse' tests/test_pages.py` returns 1
    - Source assertion: `grep -c 'app.fb_client.httpx' tests/test_pages.py` is >= 2 (mocks both .get and .post or .get plus subscribe_page_webhook)
    - Behavior: `python3 -m pytest tests/test_pages.py -k "test_oauth_start or test_state_jwt or test_callback_invalid_state or test_callback_expired_state or test_callback_webhook_failure_rollback or test_callback_success" -x -q` reports 6 passed
    - Behavior: `python3 -m pytest tests/ -q` reports 43 passed (37 existing + 6 new), 0 failed, 0 skipped
    - Behavior: in test_callback_success, the assertion `decrypt_token(access_token_enc) == "page-tok"` proves Fernet encrypt/decrypt round-trip on the actual stored value
    - Behavior: in test_callback_webhook_failure_rollback, the assertion that SELECT COUNT(*) FROM pages returns 0 proves conn.rollback() executed
  </acceptance_criteria>
  <done>
    tests/test_pages.py contains the 6 named PAGE-01 tests. All 6 pass. Full pytest suite is 43 green, 0 skipped, 0 failed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API (/auth/facebook/start) | Authenticated client JWT crosses; server must verify Bearer token before generating state |
| Facebook → API (/auth/facebook/callback) | Externally-triggered redirect carrying user-controlled `code` and `state`; server is the validator |
| API → Facebook Graph (GRAPH_BASE) | Outbound; app_secret travels in body params; never logged, never echoed |
| API → SQLite (pages table) | Page access tokens stored encrypted; queries parameterized to prevent injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Spoofing | /auth/facebook/callback — attacker crafts a callback URL with their own `code` to attach a page to a victim tenant | mitigate | `verify_oauth_state(state)` runs BEFORE any Graph API call. State JWT is HS256-signed with settings.jwt_secret, contains tenant_id + nonce + 10-min exp. Forged states fail jwt.decode → HTTPException(400). Tested by test_callback_invalid_state. |
| T-12-02 | Elevation of Privilege | State JWT replay after legitimate use | mitigate | `exp` claim set to `datetime.now(tz=timezone.utc) + timedelta(minutes=10)` in create_oauth_state. jwt.ExpiredSignatureError caught → HTTPException(400). Tested by test_callback_expired_state. |
| T-12-03 | Information Disclosure | Page access token stored in plaintext could be exfiltrated via SQL injection or DB compromise | mitigate | encrypt_token() (Fernet AES-128-CBC + HMAC) is called on every Page access_token before INSERT/UPDATE on pages.access_token_enc. Tested by test_callback_success (decrypt round-trip + non-equality with plaintext). |
| T-12-04 | Information Disclosure | fb_app_secret leaked via logs, error messages, or API response bodies | mitigate | fb_app_secret used only as an httpx params value inside app/fb_client.py function bodies. No print/log statements in fb_client.py. RuntimeError guard at function entry references the variable name, not the value. HTTPException details echo only `data["error"]["message"]` from Graph API responses, which never contain the app secret. |
| T-12-05 | Tampering | SQL injection via page_fb_id from Graph API response (technically attacker-controlled if FB account is compromised) | mitigate | All SQL in app/routers/pages.py uses `?` placeholders. Acceptance criterion explicitly greps for f-strings in SQL and rejects matches. |
| T-12-06 | Tampering | Atomicity — DB INSERT succeeds but webhook subscription fails, leaving orphan row | mitigate | `_store_pages_and_subscribe` wraps INSERT + subscribe_page_webhook in a single try block. Any exception triggers conn.rollback() before re-raise. Tested by test_callback_webhook_failure_rollback (asserts 0 rows after simulated subscription failure). Inverse (webhook subscribed, DB commit fails) is documented in RESEARCH.md Assumption A3 as acceptable for MVP — orphan resolved by next reconnect. |
| T-12-07 | Spoofing | redirect_uri mismatch (Pitfall 1) could be exploited by an attacker who registers a different domain | accept | Mitigation is configuration-level: FACEBOOK_REDIRECT_URI must exactly match Meta App Dashboard registration. Acceptance documented in user_setup frontmatter. Code uses settings.fb_redirect_uri consistently; no string-template variation. Not a code-side threat. |
| T-12-08 | Repudiation | No audit log of which tenant connected which page | accept | Audit log is in REQUIREMENTS.md "Future Requirements (deferred) — Audit log". Out of scope for Phase 12. |
</threat_model>

<verification>
- `python3 -m pytest tests/ -q` reports 43 passed (37 pre-existing + 6 PAGE-01), 0 skipped, 0 failed
- `python3 -c "from app.main import app; paths={getattr(r,'path',None) for r in app.routes}; assert '/auth/facebook/start' in paths and '/auth/facebook/callback' in paths"` exits 0
- `grep -c 'GRAPH_BASE = "https://graph.facebook.com/v25.0"' app/fb_client.py` returns 1 (Graph API v25.0 enforced)
- `grep -c 'timedelta(minutes=10)' app/routers/pages.py` returns 1 (state JWT 10-minute expiry enforced)
- No f-strings used for SQL parameter values in app/routers/pages.py (`?` placeholders only)
- fb_app_secret appears only as a value inside function bodies — never in error response details, prints, or module-level statements
</verification>

<success_criteria>
PAGE-01 is fully implementable: GET /auth/facebook/start returns a 302 to Facebook v25.0 OAuth dialog with a state JWT carrying tenant_id + nonce + 10-min exp; GET /auth/facebook/callback validates the state, executes the 3-step token exchange, encrypts each Page access token via Fernet, UPSERTs into the pages table, and subscribes each page's webhook — all in a single transaction that rolls back on subscription failure. Six PAGE-01 tests prove the happy path, two failure paths (invalid/expired state), the webhook-rollback contract, and the success/UPSERT path with Fernet round-trip.
</success_criteria>

<output>
After completion, create `.planning/phases/12-page-connection-api-facebook-oauth/12-01-SUMMARY.md`
</output>
