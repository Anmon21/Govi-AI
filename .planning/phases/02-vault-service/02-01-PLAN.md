---
phase: 02-vault-service
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - requirements.txt
  - app/config.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_content.py
  - tests/test_health.py
autonomous: true
requirements:
  - VAULT-01
  - VAULT-02
  - VAULT-03
tags:
  - python
  - fastapi
  - testing
  - vault
mvp_mode: true
walking_skeleton: false

must_haves:
  truths:
    - "Running `pip install -r requirements.txt` installs python-frontmatter and pytest"
    - "Running `python -m pytest tests/ -q` discovers and runs the test suite (tests fail because implementation does not exist yet — failures are expected and prove the contract)"
    - "`settings.vault_path` is a valid attribute on the Settings singleton (no AttributeError when tests monkeypatch it)"
    - "Tests cover all four phase success criteria: vault load at startup, GET /content/{id}, enabled filter, POST /content/reload"
  artifacts:
    - path: "requirements.txt"
      provides: "python-frontmatter and pytest dependencies"
      contains: "python-frontmatter"
    - path: "app/config.py"
      provides: "Settings.vault_path field"
      contains: "vault_path: str"
    - path: "tests/conftest.py"
      provides: "tmp_path-based vault fixture and TestClient factory"
      contains: "monkeypatch"
    - path: "tests/test_content.py"
      provides: "Failing test cases for VAULT-01, VAULT-02, VAULT-03, and POST /content/reload"
      min_lines: 80
    - path: "tests/test_health.py"
      provides: "Failing test cases for extended /health response shape"
      min_lines: 20
  key_links:
    - from: "tests/test_content.py"
      to: "app.routers.content (not yet existing)"
      via: "import app.routers.content as content_module"
      pattern: "import app\\.routers\\.content"
    - from: "tests/conftest.py"
      to: "app.config.settings"
      via: "monkeypatch.setattr(config_module.settings, 'vault_path', ...)"
      pattern: "monkeypatch\\.setattr"
---

## Phase Goal

**As a** Govi developer, **I want to** load Q&A content from an Obsidian vault and serve it by stable ID via HTTP, **so that** the bot (in Phase 3) and admins (via curl) can read content without code changes or server restarts.

<objective>
Wave 0 of the Phase 2 vertical slice. Install the new dependencies (python-frontmatter, pytest), add the `vault_path` config field, and write the failing test suite that defines the contract for VAULT-01, VAULT-02, VAULT-03, and the POST /content/reload endpoint.

Purpose: Lock the executable contract before implementation. Tests describe exactly what `/content/{id}`, `/content/reload`, and the extended `/health` endpoint must do — when Plan 02 lands, these tests turn GREEN with no spec ambiguity.

Output: requirements.txt updated, `vault_path` field on Settings, `tests/` package with conftest fixtures and failing test stubs that import `app.routers.content` (which does not exist yet — collection errors during this plan are expected and acceptable; tests must be runnable in Plan 02 without modification).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-vault-service/02-CONTEXT.md
@.planning/phases/02-vault-service/02-RESEARCH.md
@.planning/phases/02-vault-service/02-PATTERNS.md
@.planning/phases/02-vault-service/02-VALIDATION.md
@app/config.py
@app/routers/ai.py
@app/routers/health.py
@app/main.py
@requirements.txt

<interfaces>
<!-- Contracts the implementation in Plan 02 must satisfy. Tests in this plan encode these. -->
<!-- Executor: when writing tests, reference these exact names and shapes. -->

From app/config.py (after this plan):
  Settings.vault_path: str = ""    # empty string is the soft-fail trigger (D-03)

From app/routers/content.py (NOT yet implemented — Plan 02 creates this):
  router: APIRouter with prefix="/content", tags=["content"]
  _vault: dict[str, dict]   # module-level cache, keys are frontmatter `id` values
  load_vault() -> dict[str, dict]   # reads VAULT_PATH and returns a fresh dict
  ContentResponse(BaseModel): id: str, type: str, title: str, body: str
  GET /content/{content_id} -> ContentResponse  (404 if missing)
  POST /content/reload -> {"reloaded": True, "content_count": int}

From app/routers/health.py (extended in Plan 02):
  HealthResponse(BaseModel): status: str, vault_loaded: bool, content_count: int
  GET /health -> HealthResponse

Frontmatter schema (per VAULT-03):
  id: str         (required, non-empty, used as cache key)
  type: str       (required, non-empty)
  title: str      (required, non-empty)
  enabled: bool   (required; must be Python True — strings like "true" do not count, per RESEARCH Pitfall 3)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 2 dependencies and vault_path config</name>
  <files>requirements.txt, app/config.py</files>
  <read_first>
    - requirements.txt (existing 7-line dependency list)
    - app/config.py (existing Settings class — see how anthropic_api_key, app_env, app_port follow `field: type = default` style)
    - .planning/phases/02-vault-service/02-PATTERNS.md (sections "app/config.py" and "requirements.txt" — exact additions to make)
    - .planning/phases/02-vault-service/02-RESEARCH.md (Standard Stack section — pinned versions)
  </read_first>
  <action>
    Append two lines to requirements.txt, preserving existing entries and trailing newline conventions:
      python-frontmatter>=1.1.0
      pytest>=9.0.3
    Do NOT add httpx (already present at >=0.27.0 on line 6). Do NOT reorder existing lines.

    Edit app/config.py: insert one new field `vault_path: str = ""` into the Settings class, immediately after the `app_port: int = 8000` line and before `model_config`. Match the existing 4-space indent and `field_name: type = default` style. Do not change anything else in the file.

    Install the dependencies into the active environment so subsequent tasks can run pytest: `pip install -r requirements.txt`. If pip is bound to a virtualenv at .venv/ or venv/ activate it first; otherwise install into the user's current python3.

    Implements VAULT-01 prerequisite (config field) per D-01..D-03 (vault_path empty triggers soft-fail).
  </action>
  <verify>
    <automated>grep -q '^python-frontmatter>=1\.1\.0' requirements.txt && grep -q '^pytest>=9\.0\.3' requirements.txt && python -c "from app.config import settings; assert hasattr(settings, 'vault_path') and settings.vault_path == '', 'vault_path missing or wrong default'" && python -c "import frontmatter, pytest; print('deps ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^python-frontmatter' requirements.txt` outputs `1`
    - `grep -c '^pytest>=' requirements.txt` outputs `1`
    - `grep -c 'vault_path: str' app/config.py` outputs `1`
    - `python -c "from app.config import Settings; s = Settings(); assert s.vault_path == ''"` exits 0
    - `python -c "import frontmatter"` exits 0 (library importable)
    - `python -c "import pytest; print(pytest.__version__)"` prints a 9.x version string
    - app/config.py line count change is +1 (one new field added; no other edits)
  </acceptance_criteria>
  <done>
    python-frontmatter and pytest are installed and importable; Settings.vault_path defaults to "" and is reachable as `settings.vault_path`; requirements.txt and app/config.py contain only the additions specified above.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tests/ package with conftest fixtures</name>
  <files>tests/__init__.py, tests/conftest.py</files>
  <read_first>
    - app/config.py (after Task 1 — `vault_path` field must exist)
    - app/main.py (current FastAPI app construction — TestClient uses `from app.main import app`)
    - .planning/phases/02-vault-service/02-PATTERNS.md (section "tests/conftest.py" — full fixture pattern)
    - .planning/phases/02-vault-service/02-RESEARCH.md (Pattern 1 — module-level _vault scoping rules; Pitfall 1 — clear/update vs reassign)
  </read_first>
  <action>
    Create tests/__init__.py as an empty file (marks the directory as a package — matches Python convention used by app/ and app/routers/).

    Create tests/conftest.py with two pytest fixtures:

    Fixture `client(tmp_path, monkeypatch)`:
      - Imports `app.config` as `config_module` and `app.routers.content` as `content_module`.
      - Uses `monkeypatch.setattr(config_module.settings, "vault_path", str(tmp_path))` so each test has an isolated vault directory.
      - Before yielding: calls `content_module._vault.clear()` then `content_module._vault.update(content_module.load_vault())` so the in-memory cache reflects whatever .md files the test wrote to tmp_path (or is empty if the test wrote none).
      - Yields a `TestClient(app)` instance inside a `with` block (TestClient context manager triggers FastAPI lifespan correctly).
      - After yield: calls `content_module._vault.clear()` for test isolation.

    Fixture `vault_dir(tmp_path)`:
      - Returns `tmp_path` so tests that want to write files BEFORE the TestClient starts (and trigger lifespan with content already present) have a typed alias.

    Helper function `write_md(path, frontmatter_block: str, body: str = "Body")`:
      - Module-level (not a fixture). Writes `f"---\n{frontmatter_block}\n---\n{body}"` to `path` with `encoding="utf-8"`.
      - Tests import this helper to construct vault files compactly.

    The conftest MUST import `app.routers.content` at the top — this is the import that will fail (ImportError / ModuleNotFoundError) until Plan 02 creates the module. That is the expected RED state for this wave. Do not guard the import with try/except.

    Implements VAULT-01/02/03 test scaffolding per RESEARCH Pattern 1.
  </action>
  <verify>
    <automated>test -f tests/__init__.py && grep -q 'monkeypatch.setattr' tests/conftest.py && grep -q '_vault.clear' tests/conftest.py && grep -q 'def write_md' tests/conftest.py && grep -q 'TestClient(app)' tests/conftest.py</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/__init__.py` exits 0 and file is empty (`[ ! -s tests/__init__.py ]` exits 0)
    - `grep -c 'def client' tests/conftest.py` outputs `1`
    - `grep -c 'def vault_dir' tests/conftest.py` outputs `1`
    - `grep -c 'def write_md' tests/conftest.py` outputs `1`
    - `grep -q 'from fastapi.testclient import TestClient' tests/conftest.py`
    - `grep -q 'import app.routers.content' tests/conftest.py` (this import will error at collection time until Plan 02 — expected)
    - `grep -q 'monkeypatch.setattr(config_module.settings, .vault_path' tests/conftest.py`
    - No usage of `try:` around the `app.routers.content` import
  </acceptance_criteria>
  <done>
    tests/ is a Python package containing a conftest.py that defines `client`, `vault_dir`, and `write_md`. The fixtures override `settings.vault_path` per test, reload the in-memory vault before yielding TestClient, and clean up afterward.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write failing test stubs for VAULT-01, VAULT-02, VAULT-03 and reload endpoint</name>
  <files>tests/test_content.py, tests/test_health.py</files>
  <read_first>
    - tests/conftest.py (just created — references `client` fixture, `vault_dir`, `write_md` helper)
    - .planning/phases/02-vault-service/02-PATTERNS.md (sections "tests/test_content.py" and "tests/test_health.py" — full test bodies)
    - .planning/phases/02-vault-service/02-RESEARCH.md ("Phase Requirements → Test Map" — 8 required test cases)
    - .planning/REQUIREMENTS.md (VAULT-01, VAULT-02, VAULT-03 wording — assertions reference these IDs in docstrings)
  </read_first>
  <action>
    Create tests/test_content.py with the following test functions (each function name must match exactly so the verify commands work):

      test_vault_loads_at_startup  — writes one enabled .md to vault_dir, asserts `client.get("/health").json()["content_count"] == 1`. Covers VAULT-01 (startup load).
      test_get_content_by_id        — writes one enabled .md with id `q1`, title `Test Title`, body `Answer body`; asserts GET /content/q1 returns 200 with matching `id`, `title`, `body`. Covers VAULT-02 (serve by ID).
      test_get_content_missing_returns_404  — asserts GET /content/nonexistent returns 404. Covers VAULT-02 (not-found semantics).
      test_missing_vault_soft_fails — monkeypatches `settings.vault_path` to `/does/not/exist`, asserts /health returns 200 with `content_count == 0` and `vault_loaded is False`. Covers VAULT-01 (D-03 soft-fail).
      test_disabled_files_excluded  — writes one enabled (id=e1) and one disabled (id=d1, enabled: false) .md; asserts GET /content/e1 returns 200 AND GET /content/d1 returns 404. Covers VAULT-03 (enabled filter).
      test_malformed_frontmatter_skipped — writes one valid .md (id=ok1) and one with `enabled: true` but missing id/type/title; asserts content_count is 1 (bad file silently skipped, server does not crash). Covers VAULT-03 (graceful skip).
      test_enabled_string_true_rejected — writes one .md with `enabled: "true"` (quoted string, not bool); asserts the file is NOT served (GET /content/{id} returns 404). Covers VAULT-03 Pitfall 3 (strict bool check).
      test_reload_endpoint — starts with empty vault_dir, asserts content_count is 0; writes a new .md to vault_dir; POSTs /content/reload; asserts response is 200 with `{"reloaded": True, "content_count": 1}`; asserts GET /content/{new_id} now returns 200. Covers POST /content/reload (Phase 2 success criterion 4).

    Each test must use the `client` fixture (or directly monkeypatch + TestClient when testing the missing-vault soft-fail). Use the `write_md` helper from conftest for writing .md files.

    Create tests/test_health.py with:
      test_health_includes_vault_stats — with empty vault_path, GET /health returns 200 and the JSON response contains keys `status`, `vault_loaded`, `content_count`. Asserts `status == "ok"`, `vault_loaded is False`, `content_count == 0`.
      test_health_vault_loaded_true_when_vault_reachable — writes one enabled .md; asserts `vault_loaded is True` and `content_count == 1`.

    All tests MUST fail at collection time today (because `app.routers.content` does not exist). That is the RED state — Plan 02 turns them GREEN.

    Implements VAULT-01, VAULT-02, VAULT-03 verification contract.
  </action>
  <verify>
    <automated>grep -c '^def test_' tests/test_content.py | grep -qE '^(8|9)$' && grep -c '^def test_' tests/test_health.py | grep -qE '^(2|3)$' && python -m pytest tests/ --collect-only -q 2>&1 | grep -qE '(error|ModuleNotFoundError|ImportError)' && echo "RED state confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_content.py` contains exactly these function names (one per line): test_vault_loads_at_startup, test_get_content_by_id, test_get_content_missing_returns_404, test_missing_vault_soft_fails, test_disabled_files_excluded, test_malformed_frontmatter_skipped, test_enabled_string_true_rejected, test_reload_endpoint. Verify with `grep -E '^def (test_vault_loads_at_startup|test_get_content_by_id|test_get_content_missing_returns_404|test_missing_vault_soft_fails|test_disabled_files_excluded|test_malformed_frontmatter_skipped|test_enabled_string_true_rejected|test_reload_endpoint)\b' tests/test_content.py | wc -l` outputs `8`
    - `grep -E '^def (test_health_includes_vault_stats|test_health_vault_loaded_true_when_vault_reachable)\b' tests/test_health.py | wc -l` outputs `2`
    - Every test function uses either `client` fixture (parameter) or constructs `TestClient(app)` directly
    - `python -m pytest tests/ --collect-only -q 2>&1` includes either "ModuleNotFoundError: No module named 'app.routers.content'" or an equivalent ImportError — confirming RED state
    - No test contains `pytest.skip` or `pytest.xfail` markers (tests must legitimately RED, not skipped)
  </acceptance_criteria>
  <done>
    Eight content tests and two health tests are written, importing the not-yet-existing `app.routers.content` module. Running pytest produces collection errors today — those errors disappear once Plan 02 lands and all ten tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → process | The vault directory (`VAULT_PATH`) is the only file-I/O source. In Wave 0, only tests touch the filesystem (via `tmp_path`); operator-supplied paths are not yet read. |
| dev environment → tests | Pytest fixtures monkeypatch `settings.vault_path` per test. No production secrets are touched by tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Tampering | tests/conftest.py override of `settings.vault_path` | accept | `monkeypatch` auto-restores after each test; no global mutation persists. Tests use `tmp_path` only — never real vault paths. |
| T-2-02 | Information Disclosure | failing test output | accept | Tests deliberately fail at collection time in Wave 0. No secrets are logged because no .env values are loaded by the test paths. |
| T-2-04 | Denial of Service | `python -m pytest` collection scanning new tests | accept | Test suite is small (~10 tests, ~5s runtime per VALIDATION.md). No external network or service calls. |

Note: The substantive Phase 2 threats (path traversal via VAULT_PATH, malformed frontmatter injection, info disclosure in /health, unauthenticated /content/reload) materialise only when the implementation lands — they are addressed in Plan 02's threat model. This plan ships test scaffolding and a config field; nothing user-facing is reachable yet.
</threat_model>

<verification>
After all three tasks:

1. `pip install -r requirements.txt` succeeds and installs python-frontmatter + pytest
2. `python -c "from app.config import settings; print(settings.vault_path)"` prints an empty string (default)
3. `python -m pytest tests/ --collect-only -q` emits ModuleNotFoundError for `app.routers.content` (RED state — proves test contract exists ahead of implementation)
4. tests/ directory contains: `__init__.py`, `conftest.py`, `test_content.py`, `test_health.py`
5. No existing file outside the `files_modified` list has been edited (`git diff --stat` shows only requirements.txt, app/config.py, and new tests/ files)
</verification>

<success_criteria>
- requirements.txt declares `python-frontmatter>=1.1.0` and `pytest>=9.0.3`
- `app.config.settings.vault_path` is a `str` attribute defaulting to `""`
- tests/conftest.py exports `client`, `vault_dir` fixtures and `write_md` helper
- tests/test_content.py defines 8 named test functions covering VAULT-01, VAULT-02, VAULT-03, and POST /content/reload
- tests/test_health.py defines 2 named test functions covering the extended /health response
- Test collection produces an ImportError for `app.routers.content` (RED state)
- No production code under `app/routers/` or `app/main.py` has been modified (those belong to Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/02-vault-service/02-01-SUMMARY.md` capturing:
- Final python-frontmatter and pytest versions actually installed (from `pip show`)
- Exact list of test function names created (for Plan 02 GREEN-cycle reference)
- Confirmation that `app.routers.content` import still fails (the expected RED state Plan 02 will resolve)
</output>
