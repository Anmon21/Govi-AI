---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/db.py
  - tests/test_db_foundation.py
autonomous: true
requirements:
  - DB-01
must_haves:
  truths:
    - "init_schema(db_path) creates all four tables (tenants, pages, page_configs, qa_items) with correct FK columns"
    - "A connection from get_connection(db_path) has foreign_keys=ON and busy_timeout=5000"
    - "After init_schema runs once on a file-based DB, PRAGMA journal_mode returns 'wal' on subsequent connections"
    - "Running init_schema a second time on the same DB does not raise (idempotent via IF NOT EXISTS)"
    - "Foreign key violations raise sqlite3.IntegrityError on connections returned by get_connection"
  artifacts:
    - path: app/db.py
      provides: "SQLite connection factory and schema initialization"
      exports: ["get_connection", "init_schema"]
      contains: "def get_connection(db_path: str)"
    - path: tests/test_db_foundation.py
      provides: "Automated coverage for DB-01 schema, WAL, busy_timeout"
      contains: "def test_schema_creates_all_tables"
  key_links:
    - from: app/db.py
      to: sqlite3 stdlib
      via: "sqlite3.connect with check_same_thread=False"
      pattern: "sqlite3\\.connect"
    - from: tests/test_db_foundation.py
      to: app/db.py
      via: "from app.db import get_connection, init_schema"
      pattern: "from app\\.db import"
---

<objective>
Create the SQLite data layer foundation: a connection factory that applies all per-connection PRAGMAs and an idempotent schema initializer that creates the four v1.2 tables (tenants, pages, page_configs, qa_items) with correct foreign keys, indexes, and CHECK constraints. Also lay down the Wave 0 test scaffold covering schema shape, WAL mode, and busy_timeout from the DB-01 success criteria.

Purpose: Every later v1.2 phase (11–15) reads or writes through this module. Getting connection PRAGMAs and table FKs correct now prevents silent foreign-key violations and thread-safety bugs in downstream phases.

Output: `app/db.py` exporting `get_connection(db_path)` and `init_schema(db_path)`, plus `tests/test_db_foundation.py` with three failing-then-passing tests for DB-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-db-foundation/10-RESEARCH.md
@.planning/phases/10-db-foundation/10-PATTERNS.md
@.planning/phases/10-db-foundation/10-VALIDATION.md
@CLAUDE.md

# Existing test analogs — match style (plain functions, tmp_path, docstring with REQ ID)
@tests/test_health.py
@tests/conftest.py

# Existing module style analog — short module, plain functions, absolute imports from app/
@app/config.py
</context>

<interfaces>
<!-- Contracts this plan establishes. Plan 02 imports from app.db; Plan 03 calls init_schema. -->

From app/db.py (NEW — this plan creates):
```python
import sqlite3

def get_connection(db_path: str) -> sqlite3.Connection: ...
def init_schema(db_path: str) -> None: ...
```

From app/config.py (read-only in this plan — used by NO code in this plan; modified by Plan 02):
```python
class Settings(BaseSettings):
    anthropic_api_key: str = ""
    app_env: str = "development"
    app_port: int = 8000
    vault_path: str = ""
    model_config = {"env_file": ".env"}

settings = Settings()
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — create failing test scaffold for DB-01 schema, WAL, and busy_timeout</name>
  <files>tests/test_db_foundation.py</files>
  <read_first>
    - tests/test_health.py (style analog — plain pytest functions, docstring per test with REQ ID, monkeypatch + tmp_path usage)
    - tests/conftest.py (existing fixtures — confirm we do NOT need to add any; tmp_path is a pytest builtin)
    - .planning/phases/10-db-foundation/10-VALIDATION.md (sampling rate and per-task verification map)
    - .planning/phases/10-db-foundation/10-PATTERNS.md (verified test patterns in the "tests/test_db_foundation.py" section)
  </read_first>
  <behavior>
    - test_schema_creates_all_tables(tmp_path): calls init_schema on a tmp_path .db file, then queries sqlite_master and asserts {"tenants","pages","page_configs","qa_items"} is a subset of the table names returned.
    - test_wal_mode_enabled(tmp_path): after init_schema runs on a file-based DB, a new sqlite3 connection returns ("wal",) for PRAGMA journal_mode.
    - test_busy_timeout(tmp_path): a connection returned by get_connection has PRAGMA busy_timeout == 5000.
    - All three tests will fail at this stage because app/db.py does not yet exist; Task 2 makes them pass (RED → GREEN).
  </behavior>
  <action>
    Create tests/test_db_foundation.py. Use absolute imports `from app.db import get_connection, init_schema` and standard library `sqlite3`. Use plain function-style tests (no class), each with a docstring `"""DB-01: <description>."""`. Use the `tmp_path` pytest builtin fixture to obtain a file-based DB path — DO NOT use `:memory:` for the WAL test (per Pitfall 2 in RESEARCH.md). For test_schema_creates_all_tables, query `SELECT name FROM sqlite_master WHERE type='table'` and assert the four table names as a set subset. For test_wal_mode_enabled, open a NEW sqlite3 connection (not the one from get_connection) and assert PRAGMA journal_mode returns ("wal",). For test_busy_timeout, use get_connection(db_path) and assert PRAGMA busy_timeout returns 5000. Do not add a Fernet test in this plan — Plan 02 appends it. Do not modify tests/conftest.py.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_db_foundation.py -q 2>&1 | grep -E "(ModuleNotFoundError|ImportError|3 (failed|error))" &amp;&amp; echo OK_RED_PHASE</automated>
  </verify>
  <acceptance_criteria>
    - File tests/test_db_foundation.py exists
    - Source assertion: file contains `def test_schema_creates_all_tables(`
    - Source assertion: file contains `def test_wal_mode_enabled(`
    - Source assertion: file contains `def test_busy_timeout(`
    - Source assertion: file contains `from app.db import get_connection, init_schema`
    - Source assertion: file does NOT contain `:memory:` (per Pitfall 2)
    - Behavior: `python3 -m pytest tests/test_db_foundation.py -q` collects 3 tests and all 3 fail or error (RED phase confirmed) — expected ModuleNotFoundError on `app.db`
    - Regression: `python3 -m pytest tests/test_content.py tests/test_health.py -q` still exits 0 (unrelated suites untouched)
  </acceptance_criteria>
  <done>3 tests collected from tests/test_db_foundation.py, all currently failing (RED phase) due to missing app/db.py module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create app/db.py with get_connection() and init_schema() — make Wave 0 tests pass</name>
  <files>app/db.py</files>
  <read_first>
    - app/config.py (settings singleton pattern — even though this module does not import settings yet, follow the same import style and snake_case conventions)
    - app/routers/health.py (analog for short single-purpose module: absolute imports, no logging, plain functions)
    - tests/test_db_foundation.py (the failing tests this task must make pass — confirms exact import surface and expected PRAGMA values)
    - .planning/phases/10-db-foundation/10-RESEARCH.md (Pattern 1, Pattern 2, and Pitfalls 1, 2, 4, 5, 6)
    - .planning/phases/10-db-foundation/10-PATTERNS.md (verified DDL block for all four tables and indexes)
  </read_first>
  <behavior>
    - get_connection(db_path) returns a sqlite3.Connection with check_same_thread=False, row_factory=sqlite3.Row, busy_timeout=5000, foreign_keys=ON. Per Pitfall 4, journal_mode=WAL is NOT set per-connection here (WAL persists at file level after init_schema sets it once).
    - init_schema(db_path) opens a connection via get_connection, executes PRAGMA journal_mode=WAL once on this connection, then executescript that creates the four tables and four indexes using CREATE TABLE IF NOT EXISTS / CREATE INDEX IF NOT EXISTS, commits, and closes the connection.
    - Calling init_schema twice on the same path does not raise (idempotent).
    - A connection from get_connection rejects an INSERT into pages with a non-existent tenant_id with sqlite3.IntegrityError (because foreign_keys=ON).
  </behavior>
  <action>
    Create app/db.py with two public functions and no class. Module imports: `import sqlite3` only — no FastAPI, no settings (the connection factory takes db_path as an argument so it stays unit-testable per the PATTERNS.md analog). Use 4-space indentation, no `__all__`, no logging.

    Function 1 — `get_connection(db_path: str) -> sqlite3.Connection`: call `sqlite3.connect(db_path, check_same_thread=False)`; assign `conn.row_factory = sqlite3.Row`; execute `PRAGMA busy_timeout=5000`; execute `PRAGMA foreign_keys=ON`; return conn. Do NOT set `PRAGMA journal_mode=WAL` in this function (per RESEARCH.md Pitfall 4 — WAL persists at file level once set in init_schema).

    Function 2 — `init_schema(db_path: str) -> None`: call get_connection(db_path); execute `PRAGMA journal_mode=WAL` once on that connection; then call `conn.executescript("""...""")` with the DDL block copied EXACTLY from .planning/phases/10-db-foundation/10-PATTERNS.md (the `init_schema` code block under "app/db.py" section). That DDL creates four tables — tenants, pages, page_configs, qa_items — and four indexes — idx_pages_tenant_id, idx_page_configs_page_id, idx_qa_items_page_id, idx_qa_items_category_id. The qa_items.type column MUST have CHECK(type IN ('category', 'question')). All timestamp defaults MUST be `strftime('%Y-%m-%dT%H:%M:%SZ', 'now')`. Use CREATE TABLE IF NOT EXISTS and CREATE INDEX IF NOT EXISTS for every statement (idempotent). After executescript, call conn.commit() then conn.close().

    Threat T-10-SQL mitigation: this module exposes only parameterless DDL via executescript and never accepts user-supplied data. Future phases that consume get_connection MUST use sqlite3 `?` placeholders for any value substitution — note this in a short inline em-dash comment above get_connection: `# get_connection — callers MUST use ? placeholders, never f-strings, for SQL parameters`.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_db_foundation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - File app/db.py exists
    - Source assertion: file contains `def get_connection(`
    - Source assertion: file contains `def init_schema(`
    - Source assertion: file contains `check_same_thread=False`
    - Source assertion: file contains `PRAGMA busy_timeout=5000`
    - Source assertion: file contains `PRAGMA foreign_keys=ON`
    - Source assertion: file contains `PRAGMA journal_mode=WAL`
    - Source assertion: file contains `CHECK(type IN ('category', 'question'))`
    - Source assertion: file contains `CREATE TABLE IF NOT EXISTS tenants`
    - Source assertion: file contains `CREATE TABLE IF NOT EXISTS pages`
    - Source assertion: file contains `CREATE TABLE IF NOT EXISTS page_configs`
    - Source assertion: file contains `CREATE TABLE IF NOT EXISTS qa_items`
    - Source assertion: file contains `CREATE INDEX IF NOT EXISTS idx_pages_tenant_id`
    - Source assertion: file contains an em-dash comment mentioning `?` placeholders (T-10-SQL mitigation note)
    - Source assertion: grep -v '^#' app/db.py | grep -c "% " returns 0 (no string-format SQL); grep -v '^#' app/db.py | grep -cE "f\"|f'" returns 0 (no f-string SQL)
    - Behavior: `python3 -m pytest tests/test_db_foundation.py::test_schema_creates_all_tables -x` exits 0
    - Behavior: `python3 -m pytest tests/test_db_foundation.py::test_wal_mode_enabled -x` exits 0
    - Behavior: `python3 -m pytest tests/test_db_foundation.py::test_busy_timeout -x` exits 0
    - Behavior: `python3 -c "from app.db import init_schema; import tempfile, os; p=tempfile.mkstemp(suffix='.db')[1]; init_schema(p); init_schema(p); print('idempotent OK')"` prints `idempotent OK` (idempotency verified)
    - Regression: `python3 -m pytest tests/ -q` exits 0 (all 14+3 = 17 tests pass)
  </acceptance_criteria>
  <done>All 3 tests in tests/test_db_foundation.py pass (GREEN phase). Full test suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller (future router) → app/db.py | Future Phase 11+ code will pass SQL values into queries opened via get_connection; this is the boundary where untrusted-shaped input could reach SQL |
| Filesystem → SQLite WAL log files | The DB file and its WAL/SHM siblings live on disk; file permissions are out of scope for Phase 10 but recorded as a known accepted risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-SQL | Tampering | app/db.py — get_connection callers (future phases) | mitigate | Module only emits parameterless DDL via executescript. Inline em-dash comment above get_connection states callers MUST use sqlite3 `?` placeholders, never f-string / `%` SQL. Grep gate in acceptance_criteria: `grep -v '^#' app/db.py | grep -c "% " == 0` and `grep -v '^#' app/db.py | grep -cE 'f\"\|f'\\''' == 0`. |
| T-10-FK | Tampering | app/db.py — foreign key enforcement | mitigate | get_connection sets PRAGMA foreign_keys=ON on every connection (Pitfall 1). Test test_schema_creates_all_tables indirectly confirms FK columns exist; downstream phases that INSERT cross-table rows will surface IntegrityError if FK enforcement breaks. |
| T-10-WAL-PERM | Information Disclosure | govi.db plus .wal/.shm siblings on disk | accept | Phase 10 does not deploy or chmod the DB file; file permissions are a deployment-time concern recorded in RESEARCH.md "WAL log file left world-readable" row. No production secrets in the DB until Phase 12. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_db_foundation.py -q` — all 3 tests pass (DB-01 schema, WAL, busy_timeout)
- `python3 -m pytest tests/ -q` — full suite green (existing 14 vault/health tests + 3 new DB tests)
- `grep -n "PRAGMA " app/db.py` — confirms all three PRAGMAs present in source
- Idempotency: running init_schema twice on the same path completes without error
</verification>

<success_criteria>
- app/db.py exists and exports get_connection and init_schema
- tests/test_db_foundation.py exists with 3 DB-01 tests, all green
- Full pytest suite passes (no regression in tests/test_content.py or tests/test_health.py)
- All four tables exist with correct columns, foreign keys, and CHECK constraint
- WAL mode is set once during init_schema and persists at file level
- No SQL string-interpolation patterns in app/db.py source
</success_criteria>

<output>
After completion, create `.planning/phases/10-db-foundation/10-01-SUMMARY.md`
</output>
